# WP M2-18b — Wire the Adobe compatibility op set into the renderer

Read crates/pipeline-adobe (render_scaled/render_linear_scaled, operators, ADOBE_COMPAT.md), crates/image-core (graph, StageOp, CpuStageOp, backend selection, resident model from M2-17b), crates/pipeline-gpu (GpuStageOp delegation pattern), crates/tessera-ffi/src/backend.rs and develop.rs (renderer construction), engine-api ProcessVersion.
Implement: image-core selects the operator set by `Recipe.process_version`: native → existing ops; Adobe PV3–6 → `AdobeStageOp` (new, in image-core or pipeline-adobe) implementing `StageOp` for the stages pipeline-adobe covers (camera profile with DCP, WB, tone, colour, detail, effects) and delegating the rest (demosaic, lens, geometry, locals, output) to the native CPU/GPU ops; cache keys must include the op-set id so switching process version invalidates only downstream stages. The develop session exposes `process_version` get/set (switching native ↔ compat is a history entry) and the fidelity harness in tessera-ffi (M2-13b) uses the compat path when the recipe is Adobe. GPU: Adobe stages run on CPU for now (readback where needed, documented), keep the interactive path within budget by rendering compat at the adaptive level.
Tests: an Adobe-PV6 recipe renders through AdobeStageOp (count invocations); the same settings under native vs compat differ (they should) but both are finite and within display range; switching process version invalidates from CameraProfile onward only; image-core goldens unchanged for native; tessera-ffi session test for get/set process version with history.
`cargo test -p image-core -p pipeline-adobe -p tessera-ffi --release`, clippy -D warnings, fmt. engine-api unchanged. Allowed: Cargo.lock, Cargo.toml, crates/image-core/**, crates/pipeline-adobe/**, crates/pipeline-gpu/**, crates/tessera-ffi/src/backend.rs, crates/tessera-ffi/src/develop.rs (minimal: process_version get/set only — another package edits this file), crates/tessera-ffi/src/lrcat_fidelity.rs, crates/tessera-ffi/tests/**, tools/orchestrate/wp/M2-18b/**.
