# WP M2-21c — Export throughput targets (Opus)

Read tools/orchestrate/wp/M2-21/RESULTS.md (M2-21b findings: sensor stage runs one 256² tile per dispatch, 9–15k dispatches per band; uploads fixed; JPEG encode single-threaded 1–2 s), crates/export/src/gpu.rs, crates/image-core/src/resident_render.rs, crates/pipeline-gpu (batch.rs, resident.rs, lens.wgsl), crates/jobs/src/pressure.rs.
Targets (docs/08): 45 MP JPEG export < 1.5 s; 100 Web-preset JPEGs < 40 s (0.4 s each) on the M4. Current: 36 MP full 4.3 s; Web 1.4–2.7 s each.
Do: (1) whole-band sensor dispatches (one dispatch per stage per band with 2D workgroups, halo handled in-kernel) instead of per-tile; (2) two bands in flight (double-buffered scratch within the export budget); (3) parallel JPEG encoding: split into MCU-row stripes encoded on a rayon pool and stitched (or use a multi-threaded encoder crate with a permissive licence — check), and overlap encode with the next image's GPU work (already partly there); (4) Web-scale path: render at the pyramid level that covers the target size (skip full-res when the output is ≤ 1/2), keeping the precision gate at scale 1 and adding a gate at Web scale vs the CPU reference downsampled (tolerance docs/11 §1.3 full chain); (5) keep yielding to the viewport (the M2-21b test must stay green). Report before/after for the five fixtures and a 100-image synthetic batch (reuse fixtures ×20). Keep TESSERA_EXPORT_BACKEND=cpu.
`cargo test --workspace --release`, clippy -D warnings on export/image-core/pipeline-gpu/jobs, fmt. engine-api unchanged.
