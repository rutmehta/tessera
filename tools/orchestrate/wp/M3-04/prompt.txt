You are implementing work package M3-04. Work only inside the worktree at /Users/rutmehta/Developer/tessera/.worktrees/M3-04. Only touch allowed paths matching: Cargo.lock,Cargo.toml,crates/ml-segment/**,crates/ml-runtime/models.toml,crates/ml-runtime/**,crates/previews/**,tools/**,tools/orchestrate/wp/M3-04/**. IMPORTANT: the repo path contains a colon, which breaks cargo on macOS unless CARGO_TARGET_DIR points outside the repo. It is already exported in your environment as /Users/rutmehta/.cache/tessera-target/M3-04; keep it set for every cargo command (never build into ./target, never commit target/). Run this test command yourself before finishing: cargo test -p ml-segment --release && cargo clippy -p ml-segment --all-targets -- -D warnings && cargo fmt --check. Finish by printing a line RESULT: PASS or RESULT: FAIL <reason>.

# WP M3-04 — AI masks: segmentation models (`ml-segment`)

Read docs/09 §1 (segmentation rows), docs/01 §2.15 (AI mask kinds: subject, sky, background, people + parts, objects click/box/text), crates/ml-runtime (registry, session incl. token inputs), crates/ml-embed (how a model is registered + fetched), crates/pipeline-cpu masks (M2-08: mask rasters, refinement, cache), engine-api mask model (AI mask placeholders with model refs).
Licensing (docs/13): Apache-2.0/MIT weights only. Candidates: **U²-Net** (Apache-2.0) for subject/salient; **SAM** ViT-B or **MobileSAM** (Apache-2.0) as the promptable model (click/box), export encoder+decoder to ONNX (document the export script in tools/, register sha256; do not commit weights); a **sky/semantic** model: use a permissively licensed ADE20K SegFormer-B0 (NVIDIA SegFormer weights are under NVIDIA Source Code License-NC for some checkpoints — verify; if not permissive, fall back to deriving "sky" from the semantic classes of a permissive alternative or from U²-Net inverse + colour prior and document). People/parts: a permissive human-parsing model if available (check licences); otherwise mark as phase-2 and implement person masks via the face crate boxes expanded with SAM box prompts.
Implement `crates/ml-segment`: `subject(image)`, `sky(image)`, `background = 1 - subject`, `person(image, face_box)`, `promptable(image, clicks/boxes)`; outputs are single-channel f32 rasters at model resolution upsampled with guided filter refinement (from M2-08) to the requested level; results are stored as cached mask rasters keyed by (image hash, model id+version, prompt hash) via the previews/mask cache; a `Job` at Priority::Score for background subject/sky precompute.
Tests: synthetic images (bright disc on dark background → subject mask IoU > 0.8 with a disc; blue gradient top half → sky mask covers the top); partition report shows CoreML nodes; integration on fixtures when models are cached (skip offline); mask refinement keeps edges (test on a synthetic edge).
`cargo test -p ml-segment --release`, clippy -D warnings, fmt. Do not modify engine-api.
