You are implementing work package M3-19. Work only inside the worktree at /Users/rutmehta/Developer/tessera/.worktrees/M3-19. Only touch allowed paths matching: Cargo.lock,Cargo.toml,crates/ml-faces/**,crates/cull/**,crates/index/**,crates/sidecar/**,crates/library/**,crates/tessera-ffi/src/assist.rs,tools/orchestrate/wp/M3-19/**. IMPORTANT: the repo path contains a colon, which breaks cargo on macOS unless CARGO_TARGET_DIR points outside the repo. It is already exported in your environment as /Volumes/betterSSD/tessera-cache/target/M3-19; keep it set for every cargo command (never build into ./target, never commit target/). Run this test command yourself before finishing: cargo test -p ml-faces -p cull -p index -p sidecar --release && cargo clippy -p ml-faces -p cull -p index -p sidecar --all-targets -- -D warnings && cargo fmt --check. Finish by printing a line RESULT: PASS or RESULT: FAIL <reason>.

# WP M3-19 — People: clustering quality, naming propagation, per-person filters at library scale

Read docs/01 §1.4 (People view), docs/09 §1 (face recognition/clustering: ArcFace-class + HDBSCAN), crates/ml-faces (YuNet + SFace, cosine agglomerative clustering), crates/cull (people filters, face strip), crates/index (face/person tables), crates/library (people names), crates/tessera-ffi/src/assist.rs (people/person filters).
Do: replace the simple agglomerative clustering with HDBSCAN (implement in Rust, permissive; or a well-licensed crate — check) over SFace embeddings with cosine metric and per-cluster medoids; incremental assignment for new faces (nearest medoid within threshold, else new cluster; periodic re-cluster job); naming: assigning a name to a cluster propagates to all its faces and writes `person` keywords (opt-in XMP) and `mwg-rs` face regions to XMP (standard MWG Regions schema so Lightroom/others read them); merge/split clusters; "confirm/unconfirm" per face; suggestions for unnamed clusters similar to named ones; quality gates: ignore tiny/blurred faces for clustering but keep them assignable. Scale: 100k faces cluster in < 30 s (bench, ignored, synthetic embeddings) and search by person in < 50 ms via index. Tests: synthetic embeddings with known identities → ARI > 0.95; incremental assignment matches batch clustering on the same data; naming propagates and XMP regions round-trip through sidecar; merge/split invariants; real-model integration on a generated multi-face test image if the models are cached (skip offline).
`cargo test -p ml-faces -p cull -p index -p sidecar --release`, clippy -D warnings, fmt. engine-api unchanged (report needed fields). Allowed: Cargo.lock, Cargo.toml, crates/ml-faces/**, crates/cull/**, crates/index/**, crates/sidecar/**, crates/library/**, crates/tessera-ffi/src/assist.rs, tools/orchestrate/wp/M3-19/**.
