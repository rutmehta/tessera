You are implementing work package M2-09. Work only inside the worktree at /Users/rutmehta/Developer/tessera/.worktrees/M2-09. Only touch allowed paths matching: Cargo.lock,Cargo.toml,crates/lens/**,crates/pipeline-cpu/**,crates/raw-decode/**,crates/libraw-ffi/**,docs/13-licensing.md,tools/orchestrate/wp/M2-09/**. IMPORTANT: the repo path contains a colon, which breaks cargo on macOS unless CARGO_TARGET_DIR points outside the repo. It is already exported in your environment as /Users/rutmehta/.cache/tessera-target/M2-09; keep it set for every cargo command (never build into ./target, never commit target/). Run this test command yourself before finishing: cargo test -p lens -p pipeline-cpu -p raw-decode -p libraw-ffi --release && cargo clippy -p lens -p pipeline-cpu -p raw-decode --all-targets -- -D warnings && cargo fmt --check. Finish by printing a line RESULT: PASS or RESULT: FAIL <reason>.

# WP M2-09 — Lens corrections, auto-calibration, Upright (engine)

Read docs/07 §3 (optics, in priority order), docs/01 §2.8 and §2.10, crates/raw-decode (metadata, opcode presence flags), crates/libraw-ffi, crates/pipeline-cpu (Lens and Geometry stages, composed inverse map), crates/engine-api LensSettings/geometry settings.
Implement `crates/lens` (new workspace member) plus wiring:
1. **Embedded correction data**: parse DNG `OpcodeList1/2/3` (WarpRectilinear, FixVignetteRadial, FixBadPixels ignored) from DNG files, and vendor-embedded distortion/CA/vignetting tables where LibRaw exposes them (`libraw_lensinfo`, Canon/Sony/Fuji/Panasonic/Olympus makernotes via LibRaw's parsed fields; document coverage). Extend libraw-ffi/raw-decode as needed (allowed).
2. **Profile database**: LCP-compatible model (Brown–Conrady k1..k3, p1 p2, per-channel lateral CA scale, vignetting polynomial, sampled over focal/aperture/distance) with a loader for **lensfun** XML (data is CC-BY-SA-3.0: ship it as a downloadable data pack with attribution, not compiled in; record this in docs/13-licensing.md) and for Adobe `.lcp` files the user supplies; lookup by camera/lens make-model with fuzzy matching.
3. **Auto-calibration from the image** when no profile: radial distortion from detected straight lines (LSD-style line segments → minimise straightness), lateral CA by per-channel edge alignment across the field, vignetting from smooth background statistics; results savable as a user profile.
4. **Manual**: distortion, CA, vignette, defringe (purple/green hue ranges).
5. **Geometry composition**: lens distortion + Upright (auto: vanishing-point RANSAC from line segments; level/vertical/full modes; guided lines API) + rotate/scale/aspect/offset + crop composed into the single inverse map in pipeline-cpu's Geometry stage with Lanczos-3 (extend the existing map); lateral CA applied on the CFA plane before demosaic when exact, else after.
- Tests: synthetic barrel-distorted grid → estimated k1 within 5%; synthetic CA → per-channel scale recovered; vignette recovery on a synthetic falloff; DNG opcode parse on a hand-built opcode list; Upright on a synthetic perspective grid recovers the rotation within 0.5°; goldens unchanged with corrections off; the five fixtures render finite with auto corrections on.
`cargo test -p lens -p pipeline-cpu -p raw-decode --release`, clippy -D warnings, fmt. engine-api unchanged (report needed fields).
