# WP M5-14 — Layer styles and smart filters rendering

Read docs/02 §1.3 (smart objects + smart filters, non-destructive with masks and blend options), §1.4 (layer styles: drop/inner shadow, outer/inner glow, bevel & emboss, satin, colour/gradient/pattern overlay, stroke; global light; scale), crates/compositor (COMPOSITOR.md, document.rs: styles/smart filters stored but not rendered; render/mod.rs, render/exec.rs, render/pixel.rs — M5-08b is concurrently editing src/resident/**, gpu.rs and blend.rs: do NOT touch those; put style/filter rendering on the CPU path in new modules render/styles.rs and render/smart_filters.rs and register through render/mod.rs with minimal edits; the resident path may return Unsupported for styled layers for now, documented), crates/filters (Filter trait, halo), crates/vector (stroke/gradient fills for overlays), crates/psd (lfx2 parsed basics).
Implement: full Photoshop-order style stack per layer (interior effects with fill opacity semantics → stroke → shadows/glows outside; knockout interactions documented), global light, style scaling, blend mode/opacity per effect, contour/jitter placeholders; smart filters: an ordered filter stack on smart-object layers evaluated on the nested composite with per-filter blend options and a shared filter mask, cached by (filter params hash, source revision); Camera Raw filter stub calls pipeline-cpu on the raster (feature-gated). PSD interop for lfx2 basics + smart filter records (`SoLE`/filter effects) where psd already parses them.
Tests: each effect against hand-computed references on a small shape (shadow offset/spread/size, glow ranges, bevel highlight/shadow sides, stroke inside/center/outside, overlays), style scaling, global light propagation, smart filter cache reuse counts, filter mask limits effect, PSD round trip of a style set; goldens for existing tests unchanged.
`cargo test -p compositor -p filters --release`, clippy -D warnings, fmt. engine-api unchanged. Allowed: Cargo.lock, Cargo.toml, crates/compositor/src/render/**, crates/compositor/src/document.rs, crates/compositor/src/edit.rs, crates/compositor/src/format.rs, crates/compositor/src/psd.rs, crates/compositor/tests/**, crates/compositor/COMPOSITOR.md, crates/filters/**, crates/psd/**, tools/orchestrate/wp/M5-14/**.
