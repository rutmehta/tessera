You are implementing work package M2-28. Work only inside the worktree at /Users/rutmehta/Developer/tessera/.worktrees/M2-28. Only touch allowed paths matching: Cargo.lock,crates/index/**,crates/tessera-ffi/**,apps/mac/**,tools/orchestrate/wp/M2-28/**. IMPORTANT: the repo path contains a colon, which breaks cargo on macOS unless CARGO_TARGET_DIR points outside the repo. It is already exported in your environment as /Users/rutmehta/.cache/tessera-target/M2-28; keep it set for every cargo command (never build into ./target, never commit target/). Run this test command yourself before finishing: cargo test -p index -p tessera-ffi --release && cargo clippy -p index -p tessera-ffi --all-targets -- -D warnings && cargo fmt --check && (cd apps/mac && ./build-ffi.sh && swift build && swift test -c release -Xswiftc -enable-testing). Finish by printing a line RESULT: PASS or RESULT: FAIL <reason>.

# WP M2-28 — Incremental library updates (no full reload on new frames)

M3-12b found that every tethered frame reloads the whole library in the app (coalesced), which resets the undo history and the filter bar; the same happens after imports and index rescans. Read crates/index (scanner, events), crates/tessera-ffi (Engine events, EngineLibrary listing, session.rs cull queue, tether.rs frames), apps/mac EngineLibrary/CullController (how the grid model is rebuilt).
Do: an engine-side change feed (`ImageChange::{Added, Removed, Updated(fields)}` with a monotonically increasing sequence) emitted by the index on scan/insert/update, exposed over FFI as a callback and a `changes_since(seq)` pull; the Swift library model applies changes in place (insert cells at the right sorted/group position, update badges, remove) without rebuilding, preserving selection, undo stack and active filters; groups are recomputed incrementally for the affected burst only; the cull session's queue is updated in place. Tether, import, and background scan all use it. Tests: Rust for the feed ordering and coalescing; Swift for in-place insert/update/remove preserving selection and undo; a tether test-camera run of 20 frames keeps the undo stack and filter state (Swift). `cargo test -p index -p tessera-ffi --release`, clippy, fmt, `(cd apps/mac && ./build-ffi.sh && swift build && swift test -c release -Xswiftc -enable-testing)`. engine-api unchanged. Allowed: Cargo.lock, crates/index/**, crates/tessera-ffi/**, apps/mac/**, tools/orchestrate/wp/M2-28/**. Do not touch crates/compositor, crates/filters, crates/psd.
