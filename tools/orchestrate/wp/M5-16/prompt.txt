You are implementing work package M5-16. Work only inside the worktree at /Users/rutmehta/Developer/tessera/.worktrees/M5-16. Only touch allowed paths matching: Cargo.lock,Cargo.toml,crates/compositor/src/resident/**,crates/compositor/src/gpu.rs,crates/compositor/tests/**,crates/compositor/COMPOSITOR.md,crates/gpu-core/**,crates/color-mgmt/**,tools/orchestrate/wp/M5-16/**. IMPORTANT: the repo path contains a colon, which breaks cargo on macOS unless CARGO_TARGET_DIR points outside the repo. It is already exported in your environment as /Volumes/betterSSD/tessera-cache/target/M5-16; keep it set for every cargo command (never build into ./target, never commit target/). Run this test command yourself before finishing: cargo test -p compositor -p gpu-core -p color-mgmt --release && cargo clippy -p compositor -p gpu-core -p color-mgmt --all-targets -- -D warnings && cargo fmt --check. Finish by printing a line RESULT: PASS or RESULT: FAIL <reason>.

# WP M5-16 — Compositor presentation: RGBA16F/EDR, colour-managed present, GPU smart-object resampling

Leftovers from M5-04b/M5-08b (read tools/orchestrate/wp/M5-08b/*.md, COMPOSITOR.md §12): presentation is RGBA8 in document encoding only; the colour-management LUT is uploaded on every present; smart-object children are resampled on the CPU and rendered whole; level output buffers are allocated at full size even for viewports.
Read crates/compositor/src/resident/**, crates/gpu-core, crates/color-mgmt (managed output LUT from M2-15), crates/tessera-ffi/src/surface.rs and the develop session's EDR path (M2-22) for parity.
Do: `present` supports RGBA16F linear (extended sRGB / P3) with headroom like the develop session, colour-managed via a resident LUT uploaded once per (profile, intent) and cached; document colour profile → display transform; GPU smart-object resampling (Lanczos-3 / bilinear by level) with viewport-limited child rendering; viewport-sized output buffers. Keep bit-exactness of the RGBA8 path (tests pin it) and determinism; gate the new paths vs CPU references; bench (ignored, prints) present cost before/after.
`cargo test -p compositor -p gpu-core -p color-mgmt --release`, clippy -D warnings, fmt. engine-api unchanged. Allowed: Cargo.lock, Cargo.toml, crates/compositor/src/resident/**, crates/compositor/src/gpu.rs, crates/compositor/tests/**, crates/compositor/COMPOSITOR.md, crates/gpu-core/**, crates/color-mgmt/**, tools/orchestrate/wp/M5-16/**.
