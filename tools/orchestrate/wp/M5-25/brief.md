# WP M5-25 — Camera Raw filter: the full develop engine as a smart filter on RGB layers

Read docs/02 §13 (Camera Raw as a filter) and §5 (Camera Raw Filter row), crates/filters/src/compositor_adapter.rs (the `camera-raw-filter` feature stub is tone-only and must be replaced), crates/image-core (RgbSource from M2-29 enters the develop graph at the Demosaic checkpoint; stage memoisation), crates/pipeline-cpu + pipeline-gpu (operators after demosaic: WB/CAT, tone, curve, HSL, grading, detail, effects, lens, masks/local adjustments), crates/engine-api (Recipe/Settings, SourceKind::Rgb), crates/compositor/src/render/smart_filters.rs and resident routing (M5-23: ResidentFilterEvaluator, per-stage caches), crates/gpu-core.
Deliver `crates/filters/src/camera_raw.rs` (feature becomes default):
1. Params = a full engine-api `Settings` JSON (validated by engine-api), plus `amount`. Evaluation runs the RGB develop chain from the Demosaic checkpoint on the layer raster (linear premultiplied RGBA → unpremultiply → develop in the working space → premultiply), honouring the document's colour profile via color-mgmt (convert to the develop working space and back). Masks/local adjustments inside the settings are supported through the existing mask hooks with procedural masks only (AI masks are explicitly unsupported here; error clearly).
2. GPU path: implement `ResidentFilterEvaluator` support for camera_raw using pipeline-gpu's resident render path so the stack stays on the GPU; parity with the CPU path within the develop tolerances (per-operator ≤1e-4 linear; full chain ≤2e-3).
3. Tests: golden parity against `image-core` developing the same RGB file with the same settings (same pixels within tolerance), amount blending, profile round trip (Display P3 document), unsupported AI mask error, undo/serialisation through the compositor smart-filter stack, GPU parity. Bench: 24 MP layer, full chain CPU and GPU (ignored test, log numbers).
Update crates/filters/README or FILTERS.md with the contract and limits. Gate: `cargo test -p filters -p compositor --release && cargo clippy -p filters -p compositor --all-targets -- -D warnings && cargo fmt --check`. Allowed: Cargo.lock,crates/filters/Cargo.toml,crates/filters/src/camera_raw.rs,crates/filters/src/camera_raw_gpu.rs,crates/filters/src/compositor_adapter.rs,crates/filters/src/lib.rs,crates/filters/tests/camera_raw*.rs,crates/filters/FILTERS.md,crates/filters/README.md,tools/orchestrate/wp/M5-25/**. Other packages are concurrently editing crates/filters/src/remove.rs + distraction.rs and crates/compositor/src/{edit,document,format}.rs; do not touch those.
