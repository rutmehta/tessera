You are implementing work package M2-34. Work only inside the worktree at /Users/rutmehta/Developer/tessera/.worktrees/M2-34. Only touch allowed paths matching: Cargo.lock,Cargo.toml,tools/bench/**,bench-baseline*.json,.github/workflows/bench.yml,CONTRIBUTING.md,.gitignore,tools/orchestrate/wp/M2-34/**. IMPORTANT: the repo path contains a colon, which breaks cargo on macOS unless CARGO_TARGET_DIR points outside the repo. It is already exported in your environment as /Volumes/betterSSD/tessera-cache/target/M2-34; keep it set for every cargo command (never build into ./target, never commit target/). Run this test command yourself before finishing: cargo test -p tessera-bench --release && cargo clippy -p tessera-bench --all-targets -- -D warnings && cargo fmt --check && actionlint .github/workflows/bench.yml. Finish by printing a line RESULT: PASS or RESULT: FAIL <reason>.

# WP M2-34 — Performance benchmarks in CI with regression gates

docs/08 §3: continuous performance benchmarks on fixed hardware with regression gates for the targets table. Read docs/08, the ignored benches across crates (`grep -rn "#\[ignore\]" crates/*/tests crates/*/src | grep -i bench`), .github/workflows/ci.yml, ci.sh, tools/orchestrate/wp/M2-17b/benchmark-results.md and M2-21c/RESULTS.md for the numbers already measured.
Do: a `tools/bench/` runner (Rust bin or Python) that runs a curated set of benches with a JSON output schema (`bench, fixture, metric, value, unit, backend, host`), stores results under `bench-results/<host>/<date>.json` (git-ignored) and a `bench-baseline.json` per host (committed), compares against the baseline with per-metric tolerances (e.g. 15%), prints a table, and exits non-zero on regression when `BENCH_GATE=1`; a `--record-baseline` mode. Benches to include (CPU-only subset that can run on a GitHub macOS runner in < 10 min: index search 100k, sidecar round trip, preview pyramid, pipeline-cpu render L3, export CPU Web preset 1 image, compositor CPU 20-layer composite; and the GPU set for local runs: develop slider latency, GPU export, resident compositor dab/L2/L0). `.github/workflows/bench.yml`: nightly + manual, CPU subset, uploads the JSON as an artifact and comments a table on a tracking issue or just fails on gate; `make bench` / `tessera-bench` documented in CONTRIBUTING.md.
Tests: schema validation; comparison logic with synthetic results; runner smoke on the CPU subset locally (record numbers in the brief's RESULTS.md).
Allowed: Cargo.lock, Cargo.toml, tools/bench/**, bench-baseline*.json, .github/workflows/bench.yml, CONTRIBUTING.md, .gitignore, tools/orchestrate/wp/M2-34/**. Test: `cargo test -p tessera-bench --release && cargo clippy -p tessera-bench --all-targets -- -D warnings && cargo fmt --check && actionlint .github/workflows/bench.yml`.
