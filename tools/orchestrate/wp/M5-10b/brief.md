# WP M5-10b — Wire the engine `DocumentSession` into document mode (Opus)

Read tools/orchestrate/wp/M5-10/IMPLEMENTATION-STATUS.md ("What M5-10b does", the record name mapping table, deviations), tools/orchestrate/wp/M5-09/IMPLEMENTATION-STATUS.md and apps/mac/README.md (the `DocumentSession` API), apps/mac/Sources/TesseraCore/Document/DocumentBackend.swift and StubDocumentBackend.swift, apps/mac/Sources/Tessera/Document/DocumentWorkspace.swift and DocumentViewport.swift, apps/mac/Sources/TesseraFFI/tessera_ffi.swift (generated: `DocumentSession`, `DocumentListener`, records), Loupe/MetalLoupeView.swift (how the develop session's surfaces are attached), apps/mac/ACCEPTANCE.md §U.
This branch already contains both wp/M5-09 and wp/M5-10 (merged).
1. `apps/mac/Sources/TesseraCore/Document/EngineDocumentBackend.swift`: `EngineDocumentEngine: DocumentEngine` over `Engine` (new / open / from image; same path = same session) and `EngineDocumentBackend: DocumentBackend` wrapping `DocumentSession`, converting every record field by field per the mapping table; a listener adapter from `DocumentListener` to `DocumentBackendListener` that hops to the main actor and coalesces; surfaces: use `plan_surface`/`attach_surface`/`set_viewport` exactly as M5-09 documents them (level pixels), `detach_surfaces` on close and on viewport resize before re-attaching; straight-alpha RGBA8 as M5-09 presents.
2. `DocumentWorkspace.engine` uses the engine adapter whenever the library is engine-backed; the stub stays for `--stub-library` and tests. New Document on the engine gives a blank canvas (a transparent pixel layer named "Layer 1" so there is something to select); Edit in Layers passes the engine image id and `developed: true`.
3. Reconcile the conventions listed by M5-10: history id 0 = "as opened" (if the engine's root node id differs, map it in the adapter rather than changing either side), `set_selection_rect`/`clear_selection` history nodes, interactive + commit semantics, thumbnails keyed by `LayerNode.revision`. Any real mismatch that needs a Rust change goes into crates/tessera-ffi/src/document.rs (allowed) with a Rust test, then `./build-ffi.sh` and commit the regenerated bindings.
4. Run ACCEPTANCE.md §U part 2 yourself against the real engine using `fixtures/raw/sample.dng` (Edit in Layers, add adjustment layer, drag opacity, undo, save `.tessera-doc`, reopen, export flat PNG, open the PSD saved by `save_as`), fixing what fails; record screenshots (`screencapture -x -R` of the Tessera window only) into tools/orchestrate/wp/M5-10b/evidence/ and update §U's expectations where the engine behaves differently from the stub. Measure and record an opacity-drag frame time in the running app (the listener's `FrameInfo` timing) — target < 16 ms at the viewport level.
5. Swift tests: adapter record conversion round trips (every enum value, blend mode strings), listener coalescing, engine-vs-stub selection in the workspace; keep the existing 124 XCTest + 5 Swift Testing tests green.
`cargo test -p tessera-ffi --release`, clippy -D warnings, fmt, `(cd apps/mac && ./build-ffi.sh && swift build && swift test)`, `xcodebuild -scheme Tessera -configuration Debug -destination 'platform=macOS' -derivedDataPath "$HOME/.cache/tessera-derived-data-M5-10" build`, `Support/make-app.sh`. engine-api unchanged. Allowed: Cargo.lock, crates/tessera-ffi/**, apps/mac/**, tools/orchestrate/wp/M5-10b/**.
