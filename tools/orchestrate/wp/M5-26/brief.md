# WP M5-26 — Remaining adjustment layers (spec 02 §7) with bit-exact GPU ports

Read docs/02 §7 (Adjustment layers table with controls and implementation notes), crates/compositor/src/adjust.rs (existing: Levels, Curves, HueSaturation, Exposure, Invert, Posterize, Threshold, ChannelMixer, Luts) and how each adjustment is evaluated on CPU and ported in crates/compositor/src/resident (doc.wgsl / adjustment dispatch; the gate is bit-exact CPU/GPU via gpu-core precise compilation, see COMPOSITOR.md §12.3), crates/compositor/src/psd.rs (adjustment-layer PSD keys already mapped), crates/psd.
Add, with the documented Photoshop control sets and formulas: Brightness/Contrast (legacy + modern), Vibrance (+saturation, skin protection), Color Balance (shadows/mid/highlights, preserve luminosity), Black & White (six colour sliders + tint), Photo Filter (preset colours + custom, density, preserve luminosity), Gradient Map (gradient stops, dither, reverse; method classic/perceptual/linear), Selective Color (relative/absolute per colour group), Shadows/Highlights (amount/tone/radius per range, colour, midtone; local-radius operator), Color Lookup extensions if Luts lacks 3DL/CUBE/ICC abstract profile loading (check crates/color-mgmt), Desaturate, Equalize, Auto Tone/Contrast/Color (as one-shot computed adjustments that store their resolved parameters), Match Color (statistics transfer; source given as another layer id) and Replace Color (mask by colour with fuzziness + HSL shift). HDR Toning may be documented as not done.
Each: serde (versioned, PSD key mapping where Photoshop has one: `brit`, `vibA`, `blnc`, `blwh`, `phfl`, `grdm`, `selc`, `SoCo`…; document those that are native-only), CPU evaluation, GPU port bit-exact with the CPU, tests (formula spot checks against hand-computed values, CPU/GPU parity at L0/L2, PSD round trip for keyed ones), and COMPOSITOR.md §4 updates. Keep edits to crates/compositor/src/resident/mod.rs minimal (dispatch wiring only) — another package is editing resident/ concurrently; put new shader code in new files under resident/.
Gate: `cargo test -p compositor -p psd --release && cargo clippy -p compositor -p psd --all-targets -- -D warnings && cargo fmt --check`. Allowed: Cargo.lock,crates/compositor/src/adjust.rs,crates/compositor/src/adjust/**,crates/compositor/src/psd.rs,crates/compositor/src/resident/**,crates/compositor/tests/**,crates/compositor/COMPOSITOR.md,crates/compositor/Cargo.toml,crates/psd/**,crates/color-mgmt/src/**,tools/orchestrate/wp/M5-26/**.

## Round 2
Do not block the whole package on one operator. Deliver all point-wise adjustments first (Brightness/Contrast, Vibrance, Color Balance, Black & White, Photo Filter, Gradient Map, Selective Color, Desaturate, Equalize, Auto Tone/Contrast/Color, Match Color, Replace Color, Color Lookup extensions) with their GPU ports and tests. For Shadows/Highlights, add a neighbourhood adjustment path: crates/compositor/src/render/exec.rs (and pixel.rs) may be edited minimally so an adjustment can declare `needs_neighbourhood(radius)` and receive a padded backdrop window (reuse the tile-halo mechanism smart filters use); another package edits render/ concurrently — keep the diff small and additive. If that still cannot be done cleanly, ship Shadows/Highlights as CPU-only with a documented GPU fallback rather than omitting it. Allowed additionally: crates/compositor/src/render/exec.rs,crates/compositor/src/render/pixel.rs. End with RESULT: PASS listing exactly what is done and not done.
