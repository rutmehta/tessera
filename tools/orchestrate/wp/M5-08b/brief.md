# WP M5-08b — Compositor: hit the L0 and viewport targets (Opus)

Continue on branch wp/M5-08 (partial work committed; read tools/orchestrate/wp/M5-08/IMPLEMENTATION-STATUS.md and the original brief tools/orchestrate/wp/M5-08/brief.md). Current: full L0 composite of the 100-layer 20 MP bench = 203 ms (target < 100), 4K viewport = 84 ms (target < 8), and the full-L0 CPU/GPU chain error is 5.6e-3 (> 2e-3) from tiny sign differences amplified by Divide.
You designed the resident renderer (M5-04b). Do what §12.4 of COMPOSITOR.md planned properly: generate specialised WGSL per document structure with the blend-mode switch resolved at codegen time, cache compiled pipelines by structure hash, make the viewport path truly viewport-limited (dispatch only visible 16² blocks at the requested level; the 4K viewport must not touch the whole level), fix the Divide amplification (clamp the denominator epsilon identically on CPU and GPU, or evaluate Divide in the CPU's exact order; document) so the chain gate passes, keep determinism, and re-run the ignored bench with `TESSERA_BENCH_ASSERT=1`. Also finish the RGBA16F/colour-managed presentation and GPU smart-object resampling from the original brief if time allows.
`cargo test -p compositor -p gpu-core --release`, clippy -D warnings, fmt; the ignored bench must pass its asserts. engine-api unchanged. Allowed: crates/compositor/src/resident/**, crates/compositor/src/gpu.rs, crates/compositor/src/blend.rs (only for the Divide fix, mirrored on CPU), crates/compositor/COMPOSITOR.md, crates/compositor/tests/**, crates/gpu-core/**, tools/orchestrate/wp/M5-08*/**. Commit as "wp(M5-08b): compositor L0/viewport targets".
