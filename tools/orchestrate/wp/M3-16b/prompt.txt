You are implementing work package M3-16b. Work only inside the worktree at /Users/rutmehta/Developer/tessera/.worktrees/M3-16b. Only touch allowed paths matching: Cargo.lock,Cargo.toml,crates/ml-enhance/**,crates/image-core/**,crates/pipeline-gpu/**,crates/gpu-core/**,crates/tessera-ffi/src/develop.rs,tools/orchestrate/wp/M3-16b/**. IMPORTANT: the repo path contains a colon, which breaks cargo on macOS unless CARGO_TARGET_DIR points outside the repo. It is already exported in your environment as /Volumes/betterSSD/tessera-cache/target/M3-16b; keep it set for every cargo command (never build into ./target, never commit target/). Run this test command yourself before finishing: cargo test -p ml-enhance -p image-core -p pipeline-gpu -p tessera-ffi --release && cargo clippy -p ml-enhance -p image-core -p pipeline-gpu --all-targets -- -D warnings && cargo fmt --check. Finish by printing a line RESULT: PASS or RESULT: FAIL <reason>.

# WP M3-16b — GPU-resident CFA denoise handoff

M3-16 runs the CFA denoise net (ml-runtime/CoreML) but returns host tensors and disables resident rendering when denoise is active. Read crates/ml-enhance (CFA path), crates/image-core (Denoise stage, resident model, cache), crates/pipeline-gpu (resident sensor tiles, batch, hdr/managed output; M2-21c band rendering), crates/gpu-core, crates/ml-runtime (session output buffers).
Do: upload the denoised packed-CFA output straight into the resident sensor pages (one upload per tile, halo-aware), keep demosaic and everything downstream resident, memoise per (image, denoise params, model version) so tone edits never re-run the net, and blend Amount on the GPU; where CoreML returns to host memory, minimise copies (pinned/shared buffers); interactive develop session keeps its < 12 ms slider path with denoise on (the net runs once, cached). Bench (ignored, prints): first frame with denoise on for each fixture, then tone-only frames. Tests: resident chain with denoise equals the CPU chain within tolerance; cache reuse counts; Amount blend exactness at 0 and 1.
`cargo test -p ml-enhance -p image-core -p pipeline-gpu -p tessera-ffi --release`, clippy -D warnings, fmt. engine-api unchanged. Allowed: Cargo.lock, Cargo.toml, crates/ml-enhance/**, crates/image-core/**, crates/pipeline-gpu/**, crates/gpu-core/**, crates/tessera-ffi/src/develop.rs, tools/orchestrate/wp/M3-16b/**.
