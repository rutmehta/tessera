# WP M2-40v — Verify the People view on screen

Build the app with `apps/mac/Support/make-app.sh` (CARGO_TARGET_DIR=/Volumes/betterSSD/tessera-cache/target/M2-40v) and run apps/mac/ACCEPTANCE.md steps 130–141 (People view: naming, suggestions, merge, split, move, Person filter, face-strip names, Settings ▸ Library opt-ins) exactly as written, with an isolated `--app-dir` and the `--seed-faces` fixture the steps describe. Capture only the app window for each 📸 step. Use accessibility identifiers (Identifiers appendix) and keyboard-first interaction; report each step as pass/fail with what you observed, and list any visual defects (overlap, spacing, off-theme colours, truncated text) separately. Then, as a bonus, run Cull ▸ Analyze Faces on fixtures/raw (real models) and open People: report whether real clusters appear and what the tiles look like. Write tools/orchestrate/wp/M2-40v/verdict.md and finish with RESULT: PASS or FAIL.
