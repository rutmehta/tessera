You are implementing work package M3-16. Work only inside the worktree at /Users/rutmehta/Developer/tessera/.worktrees/M3-16. Only touch allowed paths matching: Cargo.lock,Cargo.toml,crates/ml-enhance/**,crates/ml-runtime/**,crates/pipeline-cpu/**,crates/image-core/**,crates/raw-decode/**,tools/**,tools/orchestrate/wp/M3-16/**. IMPORTANT: the repo path contains a colon, which breaks cargo on macOS unless CARGO_TARGET_DIR points outside the repo. It is already exported in your environment as /Volumes/betterSSD/tessera-cache/target/M3-16; keep it set for every cargo command (never build into ./target, never commit target/). Run this test command yourself before finishing: cargo test -p ml-enhance -p pipeline-cpu -p image-core --release && cargo clippy -p ml-enhance -p pipeline-cpu -p image-core --all-targets -- -D warnings && cargo fmt --check. Finish by printing a line RESULT: PASS or RESULT: FAIL <reason>.

# WP M3-16 — Joint raw (CFA) denoise + demosaic network (`ml-enhance`, phase 2)

Read docs/07 §2 (joint denoise+demosaic conditioned on the camera noise model, runs on packed CFA, Amount blends, local via mask), crates/ml-enhance (DRUNet RGB path, CfaDenoise trait/TODO from M3-05), crates/raw-decode (packed CFA u16, black/white, noise model fields if any; metadata), crates/pipeline-cpu (Denoise stage is raw-domain before demosaic; MHC demosaic), crates/image-core (cacheable Denoise stage), crates/ml-runtime.
No permissive pretrained CFA denoiser exists, so train one, small and honest:
1. **Data**: self-supervised on our own raws (the five fixtures + any raws the user adds under `fixtures/raw`) plus synthetic noise: fit a Poisson-Gaussian noise model per fixture from flat patches (or use metadata ISO), generate noisy/clean pairs by injecting noise into low-ISO CFA crops (clean = the original), and use Neighbor2Neighbor-style pairs for real noisy frames. Everything under permissive licences; document in `TRAINING.md`.
2. **Model**: a compact U-Net on 4-channel packed Bayer (RGGB-normalised by rotating patterns) conditioned on the noise level map, output denoised CFA (phase 2a) and, optionally, RGB directly (joint, 2b) — start with 2a so the exact MHC demosaic remains the reference; export to ONNX (opset pinned), fp16 variant; a `tools/train_cfa_denoise.py` (PyTorch, MIT-licensed code you write; do not vendor GPL training code) with a small default config that runs in < 30 min on the M4 (MPS) to produce a usable checkpoint; commit the training script and the ONNX export script, not the weights; register the produced model in models.toml via a local path + sha256 with a `local` source kind (add to the registry if missing).
3. **Integration**: the raw-domain `Denoise` stage runs the CFA net tiled with halo, conditioned on the noise map, blended by Amount, optionally masked (M2-08 masks sampled to the sensor grid); cacheable; GPU-resident output feeds demosaic; X-Trans falls back to the RGB DRUNet path (document).
Tests: noise model fit recovers synthetic parameters within 10%; a trained checkpoint (train in-test with a tiny config if no checkpoint is cached, ≤ 3 min) improves PSNR ≥ 3 dB on held-out synthetic noisy CFA crops; Amount 0 is bit-exact identity; tiling equals whole; goldens unchanged with denoise off; ONNX export loads through ml-runtime with a CoreML partition report.
`cargo test -p ml-enhance -p pipeline-cpu -p image-core --release`, clippy -D warnings, fmt. engine-api unchanged. Allowed: Cargo.lock, Cargo.toml, crates/ml-enhance/**, crates/ml-runtime/**, crates/pipeline-cpu/**, crates/image-core/**, crates/raw-decode/**, tools/**, tools/orchestrate/wp/M3-16/**.
