# WP M2-17m — Merge main into wp/M2-17 and reconcile

In this worktree (branch wp/M2-17), run `git merge main` and resolve conflicts keeping BOTH sides' behaviour. Main has gained since you branched: M2-15 (colour-mgmt: managed GPU output LUT, proofing, IOSurface presentation in pipeline-gpu/resident.rs and image-core render), M3-05 (post-demosaic denoise hook in image-core), M3-06 (depth/lens blur render variant), M2-14 (MaskHooks in image-core/mask_cache.rs), M2-09c (GPU geometry parity). Your branch changed guided-filter moment reuse, dispatch reductions in pipeline-gpu/src/batch.rs and resident.rs, and image-core render paths. Conflicting files: crates/image-core/src/render.rs, crates/image-core/src/resident_render.rs, crates/pipeline-gpu/OPERATORS.md, crates/pipeline-gpu/src/batch.rs, crates/pipeline-gpu/src/resident.rs.
Gate: `cargo test --workspace --release` fully green (use RUST_TEST_THREADS=1 if timing tests flake under load and say so), clippy -D warnings on image-core/pipeline-gpu/pipeline-cpu/tessera-ffi, fmt. Re-run the per-operator bench and record numbers in tools/orchestrate/wp/M2-17/validation.md. Commit the merge on wp/M2-17. Do not touch main.
