# WP M2-14 — Masking UI (Opus)

Read docs/01 §2.15 (masking UX), crates/pipeline-cpu masks (M2-08: mask kinds, brush, ranges, refinement), crates/ml-segment (M3-04: subject/sky/background/person/promptable), engine-api mask model, crates/tessera-ffi/src/develop.rs, apps/mac Inspector/Loupe (M2-13 panels pattern, the loupe overlay hooks used by the crop tool).
1. FFI: mask group CRUD on the develop session (add mask of kind, add/subtract/intersect components, invert, feather, refine), brush stroke streaming (points with pressure/flow/size/erase) coalesced per frame, range masks with a "pick in loupe" sampler, AI masks (subject, sky, background, person from a face box, click/box prompts) executed via ml-segment as a job with progress, mask overlay raster for the loupe (returned as a separate IOSurface or alpha plane), local sliders per group.
2. App: Masks panel (list of groups with thumbnails, kind icons, visibility, opacity/amount), a toolbar in the loupe (brush with size/feather/flow HUD, linear/radial gesture tools, range picker, AI buttons: Subject/Sky/Background/People/Objects-click), red overlay toggle (O) with colour choice, per-group local sliders (same custom NSControl as Basic), keyboard: M for masks, [ ] brush size, ⌥ to subtract, X invert.
3. ACCEPTANCE.md section for masks with fixture-based steps (subject mask on the tomato CR3, sky on the Fuji RAF, brush + local exposure, undo).
Design bar as before; drags must stay in the < 16 ms interactive path (mask raster cached; local adjust applied on the resident path). Tests: Rust for the FFI surface; Swift tests for stroke coalescing and mask list state. `cargo test -p tessera-ffi -p pipeline-cpu --release`, clippy, fmt, `(cd apps/mac && ./build-ffi.sh && swift build && swift test)`. engine-api unchanged.
