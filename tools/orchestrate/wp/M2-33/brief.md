# WP M2-33 — Public-repo documentation: README, CONTRIBUTING (DCO), ARCHITECTURE

The repo is public under Apache-2.0 (docs/13-licensing.md: DCO, no CLA). Read README.md, docs/README.md, docs/11-execution-plan.md, docs/12-stack-explainer.md, docs/13-licensing.md, docs/STATUS.md, crates/*/README.md and *.md design docs (CONTRACTS.md, COMPOSITOR.md, OPERATORS.md), apps/mac/README.md, apps/mac/DESIGN.md, tools/orchestrate/README.md.
Write, accurately from the code and docs (no invented features):
1. `README.md`: what Tessera is (one paragraph), screenshots placeholders referencing apps/mac/DESIGN.md, feature summary by area (catalog, culling, develop, masks, AI, export/print, layered editor status), build instructions (Rust toolchain, Xcode, `ci.sh`, `apps/mac/build-ffi.sh`, `make-app.sh`, fixtures fetch, model downloads on first use), the `tessera` CLI and `tessera-mcp` one-liners, licence and DCO line, links to docs.
2. `CONTRIBUTING.md`: DCO sign-off (`git commit -s`), branch/PR conventions, the crate map, the golden/tolerance testing rules (docs/11 §1.3), the licence allow-list and `cargo deny`, timing tests skipped on CI, the design-system lint for the app, how to add a model (registry, permissive licence, sha256).
3. `docs/ARCHITECTURE.md`: a current, honest map of crates and data flow (raw → pipeline stages → resident GPU → presentation/export; index/sidecar/library; ML runtime and models; compositor/psd/filters/brush/selection/vector/typography; MCP/agent), with links to each crate's design doc, and a "what is not built yet" list from docs/STATUS.md.
4. Fix any stale statements in docs/README.md and apps/mac/README.md you find (keep changes minimal and factual).
Verify every command you write actually runs (`bash ci.sh` may be skipped for time, but check the others exist). Allowed: README.md, CONTRIBUTING.md, docs/**, apps/mac/README.md, tools/orchestrate/README.md, tools/orchestrate/wp/M2-33/**. Test: `test -s README.md && test -s CONTRIBUTING.md && test -s docs/ARCHITECTURE.md && grep -q "Developer Certificate of Origin" CONTRIBUTING.md && grep -q "Apache-2.0" README.md`.
