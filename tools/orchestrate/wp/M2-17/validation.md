# M2-17 validation

## M2-17m merge reconciliation: RESULT: PASS

Merged main at 5adacead1614734e554910dd3e17d510243d6a4b into wp/M2-17.
Both sides' behavior is retained: denoise excludes residency, Bayer/X-Trans
and creative fusion remain supported, and managed ICC Display consumes the
fused scene-linear prefix rather than legacy sRGB output. Conflict decisions
are in ../M2-17m/resolution.md.

Executed with CARGO_TARGET_DIR=/Users/rutmehta/.cache/tessera-target/M2-17:

- cargo test --workspace --release: 733 passed, 0 failed, 17 ignored.
  First attempt passed, without RUST_TEST_THREADS=1.
- cargo clippy -p image-core -p pipeline-gpu -p pipeline-cpu -p tessera-ffi --all-targets -- -D warnings: passed.
- cargo fmt --check: passed.
- Real NEF operator_performance benchmark: passed, 45 unique operator/scope
  rows, three timed samples after one warmup, 493.86 seconds. Source hashes
  unchanged during measurement. Maximum absolute error: 6.22272491e-5.

Evidence: ../M2-17m/workspace-test.log, ../M2-17m/clippy.log, ../M2-17m/fmt.log,
benchmark-merge-main-complete.log and benchmark-merge-main-complete.json.
The earlier benchmark-merge-main.log was interrupted by the foreground tool
timeout and is excluded. Full CPU timings, individual samples, submission
counts and errors are in the completed log.

GPU-backend wall-time medians in milliseconds, including transfers/completion:

| Operator | L2 full | L0 region 1024 | L0 full |
|---|---:|---:|---:|
| basic_tone | 22.545 | 8.481 | 344.872 |
| curves | 25.859 | 9.135 | 355.482 |
| texture | 49.527 | 21.549 | 3477.454 |
| clarity | 51.114 | 21.803 | 8458.834 |
| dehaze | 60.646 | 26.665 | 14296.147 |
| sharpening | 58.396 | 23.925 | 874.541 |
| luminance_nr | 56.587 | 22.920 | 849.871 |
| chroma_nr | 58.998 | 24.106 | 881.483 |
| vibrance | 23.875 | 9.480 | 369.781 |
| hsl | 23.362 | 9.556 | 368.884 |
| color_grading | 23.364 | 9.427 | 367.984 |
| vignette | 26.657 | 12.027 | 529.633 |
| grain | 27.328 | 14.702 | 480.800 |
| crop_straighten | 26.897 | 13.365 | 17629.706 |
| local_blend_only | 5.710 | 9.652 | 93.362 |

Full-L0 texture, clarity, dehaze, geometry and local blend, plus L2 local blend,
reported zero GPU submissions and remain CPU fallbacks. These are not resident
slider or halo-aware regional-refinement measurements. Concurrent machine
activity prevents controlled speedup claims. This PASS covers M2-17m's merge
gate only; the original M2-17 performance gaps below remain unresolved.

## Historical M2-17 implementation status

RESULT: FAIL — partial implementation; required performance/coverage targets remain unmet.

## Latest retry: share presence moments and omit inactive fine scale

This retry adds radius-independent guided-filter moment reuse and omits the
fine-scale filter when Texture is zero. The GPU command counts are now 15 for
Texture-only (previously 16), 15 for Clarity-only (previously 23), and 21 for
combined Texture/Clarity (previously 23). CPU reference math, shader sums and
tolerances are unchanged. A real Metal regression compares optimized output
exactly against the original GPU graph and checks dispatch counts. It failed
first on the old 16-dispatch Texture path, then passed for all three cases:
`presence-reuse-red.log` and `presence-reuse-green.log`.

Ran the exact requested workspace-release / four-package Clippy / fmt chain
personally. The first run hit the previously observed preview callback timeout
in `tessera-ffi/tests/fallback.rs:56` (`presence-required-gate.log`). The complete
second run exited 0: **594 passed, 0 failed, 16 ignored**, with Clippy and fmt also
passing (`presence-required-gate-2.log`). No tests were weakened or skipped.

Fresh before/after NEF runs completed with stable source hashes and forced
artifact rebuilds. `presence-benchmark-results.md` and `.json` contain 90 verified
rows, generated by `presence-compare.py`. Before is the inherited partial
implementation at the start of this retry, not pristine HEAD. Each cell is one
timed sample after warmup, including transfers and completion. Substantial
variation in unchanged CPU timings precludes a controlled speedup claim.

| L2 operator | Before GPU-backend ms | After GPU-backend ms |
|---|---:|---:|
| Texture | 52.873 | 52.946 |
| Clarity | 65.265 | 56.187 |
| Dehaze (unchanged) | 63.862 | 62.721 |

Maximum reported CPU-reference error across the paired runs: 6.22272491e-5.
Clarity's independent 1024-square L0 crop measured 34.046 → 26.781 ms, but this
is not halo/global-statistics-aware regional refinement. Screen presence remains
over budget. All unfinished acceptance criteria below still apply, including
full-L0 CPU fallback, missing downsampled guidance and hashed per-image LUT/maps.
This retry does not establish completion of M2-17.

Scope validation found no dirty/untracked paths outside the allowlist, no local
`target/`, and no whitespace errors. The external Cargo target directory remained
set throughout. No commit or push. No Kanban task id is available in this session.

## Latest incremental retry

Retained the existing partial implementation and added three exact optimizations:

- Queue local-tone dispatches and replay one ordered compute pass per host-readback boundary, avoiding one Metal compute pass per filter dispatch. Tests exercise dependent means and a second flush.
- Replace full sorts in host airlight reduction with exact linear-time order-statistic selection. Reuse the luminance allocation across quantiles. Repeated-selection tests compare bit patterns against the sorted reference.
- Bypass the identity-curve upload/dispatch/readback after presence-only edits. The public API regression failed first with two submissions instead of one, then passed while maintaining CPU parity. Curve validation and nonidentity processing remain in place.

Focused red/green evidence: `single-pass-{red,green}.log`, `percentile-{red,green}.log`, and `neutral-curves-{red,green}.log`. The first two red tests failed to compile because the requested internal facilities were absent; the public transfer regression failed its assertion. No CPU reference math or tolerances changed.

Ran the exact requested workspace-release / four-package Clippy / fmt chain personally. First execution failed the existing preview wall-time assertion (3.5811325 seconds against 3 seconds), preserved in `retry-required-gate.log`. The complete second execution exited 0 with **593 passed, 0 failed, 16 ignored**, followed by successful Clippy and fmt: `retry-required-gate-2.log`. No test was weakened or skipped to obtain that pass.

Fresh real-NEF before/after measurements completed successfully: `retry-before.log` and `retry-after.log`. `retry-benchmark-results.md` and `.json` contain 90 verified rows (15 independent operators × three scopes × two runs). The before executable is this retry's inherited partial implementation, not pristine HEAD; production edits occurred only after it was compiled. The after executable was rebuilt. Before overlapped brief focused test/build activity, and each cell has only one timed sample after warmup, so these are not controlled speedup estimates.

L2 GPU-backend wall times in milliseconds:

| Operator | Retry before | Retry after |
|---|---:|---:|
| Texture | 268.954 | 65.362 |
| Clarity | 387.062 | 112.511 |
| Dehaze | 1098.887 | 66.171 |

Maximum reported same-input error across both runs is 6.22272491e-5. These timings include transfers and completion, not resident surface latency. They still exceed 16 ms. Full-L0 presence remains CPU-delegated, and all unfinished acceptance criteria below still apply. The original HEAD comparison is preserved separately as historical evidence, not relabelled as current code.

Final scope check: all dirty/untracked paths are allowlisted, `git diff --check` passes, no local `target/` exists, and every Cargo command retained `/Users/rutmehta/.cache/tessera-target/M2-17`. No commit or push. No Kanban task id was provided in the environment, so no board lifecycle transition was possible.

## Delivered

- Fused WGSL Tone → curves → Color → Effects → optional Display at output resolution, used by both point-stage batching and the actual resident renderer. One dispatch per output tile rather than one per stage. Tests assert transfer counts, dispatches, CPU parity, cancellation, validation, crop coordinates, levels, grain styles and repeatability.
- Resident X-Trans highlight reconstruction/clipping and demosaic, preserving CPU interpolation and integer CFA phase. Tests cover all tile phases, known samples, sparse patterns, frame edges, small frames, halos, invalid layouts and both demosaic selectors. X-Trans now enters the renderer's resident path as well as the direct surface path.
- Bounded last-parameter-set caches for curve coefficients, grading wheel directions, and effects/crop constants. Grading wheel trigonometry is prepared once per parameter change. These are parameter-equality caches, not the requested per-image hashed LUT/map cache.
- Cooperative workgroup-memory separable local-tone means, retaining the original clipped normalization and sum order. Direct shared/global comparison is bit-exact on boundary fixtures. Local-tone pipelines are lazily cached on their owning GpuContext, avoiding the original per-call pipeline creation and any global unsafe device-identity cache.
- Resident graph checkpoints retain f32 under the existing byte budget. New creative-chain tests exposed 0.003207922 max linear error with f16 checkpoints; retaining f32 passes the existing docs/11 full-chain bound (2e-3 linear / one display code) without widening it. This increases bytes per cached tile and may evict more sensor tiles. Older cache packing APIs remain available and tested.
- Added `Renderer::can_render_resident` and used it in Develop adaptive classification. Fused color/curve/effect recipes no longer automatically start two levels below screen resolution. The measured-time adaptation and pixel-budget safeguard remain enabled; unsupported/CPU heavy recipes retain their prior behavior.

CPU reference math was not changed. No commit or push was made.

## Verification

Executed personally after the last code change, exit 0:

    cargo test --workspace --release && cargo clippy -p pipeline-cpu -p pipeline-gpu -p image-core -p tessera-ffi --all-targets -- -D warnings && cargo fmt --check

Evidence: `validation-final.log`. Parsed Cargo summaries: **591 passed, 0 failed, 16 ignored**. Clippy and fmt completed successfully. Existing vendored LibRaw C++ warnings remain.

Earlier attempts exposed timing-sensitive failures: the FFI preview callback exceeded its three-second timeout, and a later preview render took 7.903772125 seconds against a three-second assertion. These were not fixed by weakening or skipping tests. Subsequent complete exact-command runs passed, first in `validation-retry.log` and then on final code in `validation-final.log`.

Failing-first development covered missing resident X-Trans operations, fused transfer count, constant preparation/cache reuse, shared-memory/pipeline reuse, renderer residency and the new adaptive capability query. Final tests include the actual Metal paths, not only shader parsing. X-Trans isolated comparisons retain <=1e-4 linear tolerance. New fused renderer tests enforce <=2e-3 linear and <=1 display code. Existing golden/fixture suites are included in the workspace gate; no claim is made that every arbitrary HDR input is bounded by these fixture tolerances.

All Cargo commands retained `CARGO_TARGET_DIR=/Users/rutmehta/.cache/tessera-target/M2-17`. No worktree target directory was used. Changes are confined to the requested allowlist.

## Real NEF performance evidence

`benchmark-results.md` contains complete per-operator before/after tables, with machine-readable `benchmark-results.json`. There are **90 actual timing rows**: 15 operators × three scopes × two completed builds. Scopes are full L2 (1845×1231), full L0 (7378×4924), and a centered independent L0 RGB crop (1024×1024).

- Before: immutable pre-change HEAD `17d5fb46cacd9ee0f7f7848f6b6d6d2bb85ea865`, extracted temporarily under this allowed work-package directory, using the same new benchmark harness. `benchmark-before-head.log`, exit 0.
- After: rebuilt live sources after explicitly cleaning pipeline-gpu/image-core artifacts. `benchmark-after-rebuilt.log`, exit 0, source hashes stable during the run. Later changes only adjusted test organization, added the capability query/adaptive classification, and documentation/scripts; standalone operator math did not change.
- One timed sample after one warmup per operator, serial CPU reference, full wall time including GPU transfers and completion. Not a median-based controlled speedup study. CPU timing variation shows conditions were not controlled between runs.
- All reported same-input maximum absolute errors are <=1e-4; largest observed is 6.22272491e-5. Zero GPU submissions identify retained CPU delegation rather than being credited as GPU acceleration.
- The first post-baseline `after-final` log reused stale shared-target artifacts, discovered through unexpected dispatch counts. It is excluded from the paired report. Both benchmark runners now explicitly invalidate the changed path-package artifacts before measurement.
- Initial concurrently edited/interrupted logs are retained only as diagnostics and excluded from the before/after comparison.

Measured after-build L2 presence wall times: Texture 122.631 ms, Clarity 90.425 ms, Dehaze 233.079 ms. Full L0 presence still delegates to CPU. Effects batching reduces L2 submissions from 80 to 6 across warmup + one measured invocation. These independent-operator measurements do not show resident slider latency or halo/global-statistics-aware regional refinement.

## Unfinished acceptance criteria

1. The universal <16 ms screen-slider and <100 ms 1:1 regional-refinement targets are **not demonstrated**. Current heavy presence measurements are far above the screen budget.
2. Downsampled guidance was not implemented. It changes CPU-reference neighborhoods/statistics; an approximation contract or a validated error-bounded scheme is still needed rather than silently changing the oracle.
3. Active Texture/Clarity/Dehaze still use a whole-image host barrier with exact host airlight reduction. Full L0 inputs exceed the current packed-buffer limit and retain CPU fallback. Shared-memory loading alone did not show an end-to-end speedup.
4. Extended geometry, local adjustment/mask preparation, oversized local blend, and nonresident X-Trans APIs retain CPU delegation. Not every remaining CPU-delegated operator was ported.
5. The requested parameter-hash-keyed per-image curve/grading LUTs and vignette pixel maps were not implemented. The delivered bounded coefficient/constants caches are narrower.
6. No controlled end-to-end before/after slider or regional benchmark proves that all heavy panels can remain at screen resolution. The standalone region benchmark must not be presented as that proof.

The test/lint/format gate passes, but the work package must not be marked complete from that fact alone.
