You are implementing work package M2-17. Work only inside the worktree at /Users/rutmehta/Developer/tessera/.worktrees/M2-17. Only touch allowed paths matching: Cargo.lock,Cargo.toml,crates/pipeline-cpu/**,crates/pipeline-gpu/**,crates/image-core/**,crates/tessera-ffi/**,tools/orchestrate/wp/M2-17/**. IMPORTANT: the repo path contains a colon, which breaks cargo on macOS unless CARGO_TARGET_DIR points outside the repo. It is already exported in your environment as /Users/rutmehta/.cache/tessera-target/M2-17; keep it set for every cargo command (never build into ./target, never commit target/). Run this test command yourself before finishing: cargo test --workspace --release && cargo clippy -p pipeline-cpu -p pipeline-gpu -p image-core -p tessera-ffi --all-targets -- -D warnings && cargo fmt --check. Finish by printing a line RESULT: PASS or RESULT: FAIL <reason>.

# WP M2-17 — Operator performance at full resolution

M2-13 measured: heavy Develop panels (Texture/Clarity/Dehaze guided-filter passes, noise reduction, tone curves with per-channel LUT rebuilds, colour grading, vignette) take 250–600 ms per frame at screen level on the 36 MP NEF, so the app drops to L4–L6 during drags. Targets (docs/08): < 16 ms at screen resolution for every slider, < 100 ms for 1:1 region refinement.
Read crates/pipeline-cpu (all M2 operators), crates/pipeline-gpu (which are ported, which delegate), crates/image-core (graph, resident model, cache), crates/tessera-ffi/src/develop.rs (adaptive level logic), tools/orchestrate/wp/M2-06*/validation.md.
Do: profile each operator (add an ignored bench that prints per-operator ms at L2 and L0 on the NEF); port the remaining CPU-delegated operators to WGSL (guided filter passes were benchmarked in the spike at ~48 ms for 16 MP: use separable box sums with shared memory, downsampled guidance for Texture/Clarity/Dehaze like Lightroom, and run at the render level not full-res); cache per-image constant work (curve LUTs, grading LUTs, vignette maps) keyed by params hash; fuse tone → color → effects into one WGSL pass at output resolution; make X-Trans demosaic resident. Keep the CPU reference as the oracle (tolerance docs/11 §1.3). Report before/after per operator.
`cargo test --workspace --release`, clippy -D warnings on pipeline-cpu/pipeline-gpu/image-core/tessera-ffi, fmt.
