# WP M2-19 — App support directory override, so tests never pollute the real index

Every app test run and computer-use acceptance run has been writing scratch folders into the user's real index at `~/Library/Application Support/Tessera` (noted by three packages). Add an override: the `TESSERA_APP_DIR` environment variable and an `--app-dir <path>` launch argument (the CLI already has `--app-dir`; reuse the same resolution rules) so the app, the Swift tests, `Support/make-app.sh` smoke tests, and `apps/mac/ACCEPTANCE.md` all use an isolated directory. Update ACCEPTANCE.md section A to export `TESSERA_APP_DIR="$SCR/appdir"` and every launch line to include `--app-dir "$SCR/appdir"`; update the Swift tests to use a temp app dir by default. Then clean the developer's real index of stale rows for paths that no longer exist: add `tessera index prune [--dry-run]` in tessera-cli (removes image/file rows whose files are missing; reports counts) and document it; do not run it against the real index yourself.
`(cd apps/mac && ./build-ffi.sh && swift build && swift test)`, `cargo test -p tessera-ffi -p tessera-cli --release`, clippy, fmt. Allowed: apps/mac/**, apps/tessera-cli/**, crates/tessera-ffi/src/lib.rs, crates/index/**, tools/orchestrate/wp/M2-19/**.
