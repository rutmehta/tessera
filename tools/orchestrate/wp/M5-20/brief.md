# WP M5-20 — engine-api 1.3: channels, people identities, recipe source kind

Read crates/engine-api (all of it, incl. CONTRACT/README and the 1.2 changelog), then the follow-up notes in tools/orchestrate/wp/M5-15/IMPLEMENTATION.md ("engine-api follow-up"), tools/orchestrate/wp/M3-19/implementation.md ("Engine API / host fields") and tools/orchestrate/wp/M2-29/REPORT.md (item 4, Recipe.source_kind). Add, backward-compatibly (1.2 documents/recipes must still parse; add fixtures proving it):
1. `ChannelId`, `ChannelKind { Alpha, Spot { display_rgb, solidity } }`, `ChannelSummary` (id, name, kind, depth, extent), document summaries exposing ordered channels independent of the active selection, `DocumentToolCall` variants add/delete/rename/edit channel and load-channel-as-selection (with the existing expect_head / rationale / history-group envelope). `SelectionId` maps to the alpha ChannelId numeric value; document that.
2. People: `PersonId`, `FaceRef { image_id, ordinal }`, `PersonSummary { id, name: Option, confirmed_count, approximate }`, face region (normalised centre/size + coordinate-space dims), `NameSuggestion { person_id, name, similarity }` (similarity is cosine, not a probability), `ClusterOptions` (cosine threshold, quality gate, refit interval), `PeopleJobResult`. Library tool calls for assign/confirm/merge/split/name with opt-ins for sidecar writes and person keywords.
3. `Recipe.source_kind: SourceKind { Raw, Rgb }` with `#[serde(default)]` and the inference policy for legacy recipes documented (absent = Raw; the `source_kind` string extension member written by M2-29 is read as an alias and normalised on save).
Bump the version constant to 1.3, update the changelog/CONTRACT doc, serde round-trip + schema-fixture tests for every new type. No other crate changes (do not wire callers; that is follow-up work). Gate: `cargo test -p engine-api --release && cargo clippy -p engine-api --all-targets -- -D warnings && cargo fmt --check && cargo check --workspace`. Allowed: crates/engine-api/**, tools/orchestrate/wp/M5-20/**.
