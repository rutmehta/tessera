# WP B5-04 — Layered editor tools: painting, selections, transform (Opus) — starts after M5-05, B5-01, B5-02 merge

Read docs/02 §3 (selection tools), §4 (brush engine, HUD, symmetry), §5 (clone/heal/patch), §6 (Free Transform), crates/brush and crates/selection (M5-05: stroke model, dynamics, ABR, dab rendering, selection tools, refine edge, contour tracing), crates/tessera-ffi/src/document.rs (B5-01), apps/mac Document/* (B5-02: viewport, `DocumentBackend`, panels), Loupe/LoupeMaskTools.swift + MaskToolbar.swift (the mask brush's stroke capture, coalescing per frame, pressure from `NSEvent.pressure`/tablet events, HUD).
1. **FFI** (crates/tessera-ffi/src/document.rs additions): `begin_stroke(layer, target: Pixels|Mask, tool: Brush|Eraser|Clone|Heal, BrushParams, colour)`, `stroke_points(Vec<StrokePoint{x,y,pressure,tilt_x,tilt_y,timestamp}>)` coalesced per frame with the dirty rect returned, `end_stroke()` producing one history node; `set_clone_source(layer, dx, dy)`; selection: `select_marquee(rect|ellipse, feather, aa, op)`, `select_lasso(points, kind: free|polygon|magnetic, op)`, `select_wand(x, y, tolerance, contiguous, sample_all, op)`, `select_quick(stroke, op)`, `select_color_range(colour, fuzziness)`, `select_subject|sky|object(x,y)` via ml-segment, `select_all/none/inverse`, `modify_selection(border|smooth|expand|contract|feather, px)`, `refine_edge(params)`, `selection_outline(level) -> Vec<Polyline>` for marching ants, `load/save_selection(channel name)`; transform: `begin_transform(layers)`, `set_transform(Affine, interpolation)` interactive, `commit_transform`/`cancel_transform`; `fill_selection(colour|content_aware placeholder)`, `delete_selection`.
2. **App**: tools palette (V move, M marquee ⇧M ellipse, L lasso ⇧L polygon/magnetic, W wand/quick select, B brush, E eraser, S clone, J heal, G gradient/fill placeholder, C crop placeholder, T type placeholder, I eyedropper, H hand, Z zoom), options bar per tool (size/hardness/opacity/flow/spacing, blend mode, pressure toggles, symmetry, sample all layers, tolerance/contiguous, feather, anti-alias), brush HUD (⌃-click / ⌥-right-drag for size and hardness, [ ] size, ⇧[ ] hardness, number keys opacity), on-canvas stroke capture with tablet pressure and tilt, cursor outline at brush size, symmetry guides, Brushes panel (preset list, ABR import, tip preview), Clone Source overlay, marching ants from `selection_outline`, selection modifiers (⇧ add, ⌥ subtract, ⇧⌥ intersect), Select ▸ menu (Subject, Sky, Color Range…, Modify ▸, Refine Edge… sheet with preview modes), Free Transform (⌘T: handles, ⇧ constrain, ⌥ from centre, rotation outside handles, numeric options bar, Return/Esc), colour picker panel and foreground/background swatches (X swap, D default).
3. ACCEPTANCE.md section with steps over `fixtures/raw/sample.dng` opened via Edit in Layers: brush stroke visible and undoable, eraser to transparency, wand selection outline, subject selection, transform commit, PSD save and reopen.
Interactive dabs must stay under 16 ms at the viewport level (M5-04b/M5-08 resident path). Swift tests: stroke coalescing and pressure curve, HUD maths, selection modifier mapping, transform matrix composition. Rust tests for every FFI call. `cargo test -p tessera-ffi --release`, clippy -D warnings, fmt, `(cd apps/mac && ./build-ffi.sh && swift build && swift test)`. engine-api unchanged. Allowed: Cargo.lock, crates/tessera-ffi/**, apps/mac/**, tools/orchestrate/wp/B5-04/**.
