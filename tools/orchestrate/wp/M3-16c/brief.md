# WP M3-16c — Restore the CFA training prerequisites and make the training test robust

`crates/ml-enhance/tests/cfa_model.rs` and `crates/ml-enhance/TRAINING.md` reference `tools/orchestrate/wp/M3-16/requirements.txt` and `tools/orchestrate/wp/M3-16/test_training.py`, which were never committed (the M3-16 worktree was removed). Recreate them: `requirements.txt` pinning torch (MPS-capable), numpy, onnx, onnxruntime (permissive licences), and `test_training.py` as the small pytest/driver the Rust test invokes (read cfa_model.rs and train_cfa_denoise.py / export_cfa_denoise.py to see exactly what it must do: tiny config, ≤ 3 min, writes a checkpoint + ONNX the Rust test loads). Move both into `crates/ml-enhance/training/` (a stable, non-orchestration path) and update the Rust test, TRAINING.md and the scripts' docs to that path. Make `cfa_model` **skip with a clear message** (not fail) when `TESSERA_TRAIN_PYTHON` is unset or the venv is missing, and always skip under `CI`; keep the full test mandatory when the env is present. Add `crates/ml-enhance/training/README.md` with the one-command venv setup (`python3 -m venv … && pip install -r …`). Set up the venv locally, run the full test once, and record the PSNR gain and the timing in tools/orchestrate/wp/M3-16c/RESULTS.md.
`cargo test -p ml-enhance --release`, clippy -D warnings, fmt. Allowed: crates/ml-enhance/**, tools/train_cfa_denoise.py, tools/export_cfa_denoise.py, tools/orchestrate/wp/M3-16c/**.
