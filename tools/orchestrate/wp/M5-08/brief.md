# WP M5-08 — Compositor L0 target: specialised shaders and viewport-limited rendering

Read crates/compositor/COMPOSITOR.md §12 (resident design, §12.4 plan), crates/compositor/src/resident/*, crates/gpu-core, tools/orchestrate/wp/M5-04b/IMPLEMENTATION-STATUS.md. Current: full L0 composite of a 100-layer 20 MP document = 198 ms (target < 100 ms); the general shader's per-layer blend-mode switch roughly doubles non-Normal cost.
Do: (1) generate a specialised WGSL shader per document structure (blend-mode sequence, group nesting, feature flags) with a small cache keyed by a structure hash, falling back to the general shader; (2) viewport-limited rendering: composite only the visible rect (+ margin) at the requested level, with the same dirty-block logic; (3) RGBA16F presentation option and colour-managed output via color-mgmt's LUT (matches the develop session's EDR path); (4) smart objects resampled on the GPU. Gate everything vs the CPU reference (docs/11 §1.3), determinism test, and the bench (ignored, prints) before/after: targets L0 < 100 ms full, viewport 4K window < 8 ms.
`cargo test -p compositor -p gpu-core --release`, clippy -D warnings, fmt. engine-api unchanged. Allowed: Cargo.lock, Cargo.toml, crates/compositor/src/resident/**, crates/compositor/src/gpu.rs, crates/compositor/COMPOSITOR.md, crates/compositor/tests/**, crates/gpu-core/**, tools/orchestrate/wp/M5-08/**. Do not touch crates/compositor/src/format.rs (another package may edit it).
