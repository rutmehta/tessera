You are implementing work package M5-12. Work only inside the worktree at /Users/rutmehta/Developer/tessera/.worktrees/M5-12. Only touch allowed paths matching: Cargo.lock,Cargo.toml,crates/typography/**,tools/orchestrate/wp/M5-12/**. IMPORTANT: the repo path contains a colon, which breaks cargo on macOS unless CARGO_TARGET_DIR points outside the repo. It is already exported in your environment as /Volumes/betterSSD/tessera-cache/target/M5-12; keep it set for every cargo command (never build into ./target, never commit target/). Run this test command yourself before finishing: cargo test -p typography --release && cargo clippy -p typography --all-targets -- -D warnings && cargo fmt --check. Finish by printing a line RESULT: PASS or RESULT: FAIL <reason>.

# WP M5-12 — Text layers (`typography`)

Read docs/02 §9 (type: point/paragraph text, character/paragraph panels, OpenType features, warp, text on path, variable fonts, live editable text), crates/compositor (text layer placeholder in the document model; do not edit compositor: expose a `TextRenderer` that produces a Raster + bounds; M5-08b is editing compositor internals), crates/psd (TySh engine data: string/font/size extraction), engine-api document types (text layer fields if any; report needed fields).
Implement `crates/typography` with permissive deps only (`cosmic-text` or `parley`+`swash`+`rustybuzz`, `fontdb`, `ttf-parser`; check licences): font discovery (system fonts via fontdb on macOS, user font dirs), a text model (runs with font family/style/size/tracking/kerning/leading/baseline shift/colour/OpenType features/variable axes; paragraph alignment, justification, hyphenation placeholder, indents, spacing; point vs paragraph box; vertical text placeholder), shaping and layout (bidi via unicode-bidi, line breaking via unicode-linebreak), rasterisation to a premultiplied RGBA raster with subpixel positioning and hinting off, anti-aliased at any zoom; warp presets (arc, flag, wave… via a mesh warp on the rasterised glyph outlines) and text on a path (from a `lyon`-style path); export of glyph outlines as vector paths (for M5-13 shapes); a stable text-engine-data JSON that round-trips the model; PSD interop hook mapping TySh basics.
Tests: layout determinism; a known string in a bundled permissive test font (e.g. a small OFL font vendored under its licence) rasterises to an expected coverage checksum; alignment/justification widths; OpenType ligature toggles change glyph count; warp identity at 0; text on a circular path places glyphs at expected angles; round trip of engine data.
`cargo test -p typography --release`, clippy -D warnings, fmt. engine-api unchanged (write NEEDS.md). Allowed: Cargo.lock, Cargo.toml, crates/typography/**, tools/orchestrate/wp/M5-12/**.
