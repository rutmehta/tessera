# WP M3-20 — Remove tool: real inpainting model (LaMa, Apache-2.0) + distraction detection hook

Read crates/filters/src/remove.rs (Remove trait, CpuPatchMatch, OnnxInpainter adapter with named inputs, stride-8 padding), crates/ml-runtime (registry, models.toml conventions, hash pinning, download-on-demand, CoreML partition guard) and how crates/ml-enhance/ml-segment register and fetch their weights and test with real cached models (`TESSERA_*_MODELS` env / cache dir), docs/13-licensing.md (only Apache-2.0/MIT/BSD weights).
1. Pick and pin a LaMa-class inpainting ONNX export whose weights and code are Apache-2.0 or MIT (advimman/lama "big-lama" is Apache-2.0; if you export it yourself, add the export script under crates/filters/training/ with a requirements.txt, mirroring crates/ml-enhance/training). Do NOT commit weights. Fill the `remove/lama` registry entry: URL(s), SHA-256, size, licence, input/output names, resolution rules. If no trustworthy pre-exported ONNX exists, export from the PyTorch checkpoint locally (document the exact commands and the resulting hash).
2. Make `OnnxInpainter` load it through ml-runtime (CoreML EP with CPU fallback), run at a bounded working resolution with dilation and seamless paste-back (gradient-domain blend at the mask boundary), and be selected by `Remove::auto` when weights are present.
3. Quality tests with the real model (skipped cleanly when weights are absent, like the other ML crates): remove a synthetic object from a textured background; assert boundary seam error and that the filled region's statistics match the surroundings better than CpuPatchMatch on the same input; a 1024×1024 run under 2 s on CoreML.
4. Distraction hook: `DistractionDetector` trait with a stub CPU implementation (wires via long thin high-contrast structures; people via ml-faces boxes dilated) producing masks the Remove pipeline consumes; test the pipeline end to end with the stub.
Gate: `cargo test -p filters -p ml-runtime --release && cargo clippy -p filters -p ml-runtime --all-targets -- -D warnings && cargo fmt --check`. Allowed: Cargo.lock,crates/filters/Cargo.toml,crates/filters/src/remove.rs,crates/filters/src/distraction.rs,crates/filters/src/lib.rs,crates/filters/tests/remove*.rs,crates/filters/tests/distraction*.rs,crates/filters/training/**,crates/ml-runtime/**,tools/orchestrate/wp/M3-20/**. Do not touch crates/filters/src/gpu.rs or compositor_adapter.rs (another package owns them).
