# WP M3-14b — Critic metrics exact at full resolution, cheap

M3-14 moved agent rendering to a GPU preview level (~50 ms/step) but the critic's clipping metric diverges: DNG clipping 0.028 at preview vs 0.118 at full resolution, because downsampling averages clipped pixels away. Read crates/agent (critic metrics), crates/tessera-mcp (Console render/get_histogram), crates/image-core (resident renderer, whole-level rendering, managed output), crates/pipeline-gpu (histogram pass from M2-22's hdr_surface.wgsl; managed output).
Do: compute clipping fractions, histogram and mean luminance on the **full-resolution** output via a GPU reduction pass inside the resident renderer (a per-tile/per-band reduction kernel that counts per-channel clipped pixels and 256-bin histograms, merged on CPU), reusing the memoised stages so a critic step costs one reduction (target ≤ 20 ms on the NEF), and keep the preview image for the VLM only; skin-tone ΔE evaluated on face boxes at full-res crops (small); document each metric's resolution. Tests: full-res metrics from the reduction equal the CPU-computed metrics on the five fixtures within 1e-4 (fractions) and exact for histogram bins; the five-fixture parity bench now passes; the agent loop tests and tessera-mcp tests stay green; step time bench printed.
`cargo test -p agent -p tessera-mcp -p image-core -p pipeline-gpu --release`, clippy -D warnings, fmt. engine-api unchanged. Allowed: Cargo.lock, Cargo.toml, crates/agent/**, crates/tessera-mcp/**, crates/image-core/**, crates/pipeline-gpu/**, tools/orchestrate/wp/M3-14b/**.
