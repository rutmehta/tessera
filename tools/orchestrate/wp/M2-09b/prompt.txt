You are implementing work package M2-09b. Work only inside the worktree at /Users/rutmehta/Developer/tessera/.worktrees/M2-09b. Only touch allowed paths matching: Cargo.lock,Cargo.toml,crates/lens/**,crates/pipeline-cpu/**,crates/pipeline-gpu/**,crates/raw-decode/**,docs/13-licensing.md,tools/orchestrate/wp/M2-09b/**. IMPORTANT: the repo path contains a colon, which breaks cargo on macOS unless CARGO_TARGET_DIR points outside the repo. It is already exported in your environment as /Users/rutmehta/.cache/tessera-target/M2-09b; keep it set for every cargo command (never build into ./target, never commit target/). Run this test command yourself before finishing: cargo test -p lens -p pipeline-cpu -p pipeline-gpu -p raw-decode --release && cargo clippy -p lens -p pipeline-cpu -p pipeline-gpu -p raw-decode --all-targets -- -D warnings && cargo fmt --check. Finish by printing a line RESULT: PASS or RESULT: FAIL <reason>.

# WP M2-09b — Lens: data pack download, DNG opcode execution in-stage, independent manual CA, GPU CA

Read crates/lens (README/HANDOFF from M2-09), crates/pipeline-cpu (lens_plan.rs, lens_resolve.rs, optics), crates/pipeline-gpu (lens.wgsl, the separate lateral-CA pass from M2-21b), crates/raw-decode (opcode lists), crates/ml-runtime registry (download/verify pattern), docs/13-licensing.md (lensfun data is CC-BY-SA-3.0: distributed as a separate downloadable pack with attribution).
Do:
1. **Lensfun data pack**: a `LensDataPack` downloaded on demand (URL of the upstream lensfun-db release tarball or a pinned GitHub archive; sha256 pinned; cache under app-dir/lens; attribution text bundled; version shown), parsed with the existing loader into the profile database; auto-update check optional; tests with a small synthetic pack.
2. **DNG opcodes executed in-stage**: WarpRectilinear (per-channel radial), FixVignetteRadial, and GainMap (Opcode 9, used by ProRAW/HEIF-wrapped raws) applied at the correct pipeline positions (OpcodeList1 before demosaic on the CFA, List2 after demosaic in linear, List3 after colour), with priority per docs/07 §3 (embedded first); tests on hand-built opcode lists and, when present, real fixtures with opcodes (none of the five have them; document).
3. **Manual CA independent controls** (red/cyan, blue/yellow amounts; defringe purple/green hue ranges) as separate settings mapped from `crs:` keys (check the crs table for names), applied in the lateral-CA pass.
4. **GPU lateral CA folded correctly**: keep the sensor-frame CA pass exact, but batch it with the demosaic dispatch to avoid a separate readback; gate vs CPU.
`cargo test -p lens -p pipeline-cpu -p pipeline-gpu -p raw-decode --release`, clippy -D warnings, fmt. engine-api unchanged (report needed fields). Allowed: Cargo.lock, Cargo.toml, crates/lens/**, crates/pipeline-cpu/**, crates/pipeline-gpu/**, crates/raw-decode/**, docs/13-licensing.md, tools/orchestrate/wp/M2-09b/**.
