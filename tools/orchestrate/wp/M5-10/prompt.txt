You are implementing work package M5-10. Work only inside the worktree at /Users/rutmehta/Developer/tessera/.worktrees/M5-10. Only touch allowed paths matching: Cargo.lock,Cargo.toml,crates/tessera-mcp/**,apps/tessera-cli/**,crates/brush/**,crates/selection/**,tools/orchestrate/wp/M5-10/**. IMPORTANT: the repo path contains a colon, which breaks cargo on macOS unless CARGO_TARGET_DIR points outside the repo. It is already exported in your environment as /Volumes/betterSSD/tessera-cache/target/M5-10; keep it set for every cargo command (never build into ./target, never commit target/). Run this test command yourself before finishing: cargo test -p tessera-mcp -p tessera-cli --release && cargo clippy -p tessera-mcp -p tessera-cli --all-targets -- -D warnings && cargo fmt --check. Finish by printing a line RESULT: PASS or RESULT: FAIL <reason>.

# WP M5-10 — Wire brush and selection crates into the document executor

Read crates/tessera-mcp (Documents, BrushEngine/SelectionEngine traits, RoundBrush/BasicSelection built-ins, README gaps), crates/brush (api::paint_stroke, tile deltas, ABR, heal/clone), crates/selection (api::set_pixel_selection, tools, refine edge, alpha channels, contour tracing), engine-api 1.2 (SelectionShape, DocumentToolCall), crates/compositor (DocOp::PaintTiles, selections).
Do: implement `BrushEngine` with `brush` (all tips/dynamics via a brush preset id resolved from a preset store under app-dir, ABR import tool `import_brushes`), `SelectionEngine` with `selection` (all shapes engine-api supports; add magic wand/quick selection/colour range/object/subject/sky via a new additive `SelectionShape` variants set in engine-api ONLY if the contract owner allows — otherwise expose them as `select_advanced` params in tessera-mcp and list the needed contract fields in a NEEDS.md), refine-edge and boolean ops as tools, saved selections tracked in history (undo removes), undo/redo MCP tools, ICC embedding on image export via color-mgmt, PSD import warnings surfaced in the response. Tests: paint through the real brush produces the same pixels as calling brush directly; ABR import lists presets; wand/quick select over MCP; undo removes a saved selection; export embeds ICC.
`cargo test -p tessera-mcp -p tessera-cli --release`, clippy -D warnings, fmt. Allowed: Cargo.lock, Cargo.toml, crates/tessera-mcp/**, apps/tessera-cli/**, crates/brush/**, crates/selection/**, tools/orchestrate/wp/M5-10/**. engine-api unchanged (write NEEDS.md).
