# WP M2-05 — GPU ports of the Milestone 2 operators (`pipeline-gpu`)

**Main currently fails to compile**: `crates/pipeline-gpu` has a non-exhaustive match over `image_core`'s `Op` enum after M2-04 added `Op::Detail`, `Op::ToneExtra`, `Op::Color`, and three more (see `cargo build -p pipeline-gpu`). Step 1 (do this first, commit it): make `GpuStageOp` compile by delegating every new op to the CPU implementation (`CpuStageOp`) so the whole workspace builds and `cargo test --workspace --release` is green again. Step 2: port to WGSL compute, in this order, each gated against the CPU reference at the docs/11 §1.3 tolerance with tests: point/parametric tone curves, Vibrance/Saturation/HSL/Color Grading in OkLCh, vignette + grain, unsharp mask + luminance/chroma NR, Texture/Clarity/Dehaze (guided filter passes; reuse the spike's separable box sums), crop/straighten inverse map with Lanczos-3. Keep the CPU delegation for anything not yet ported. Update pipeline-gpu/OPERATORS.md and the ignored bench (tone-only and full-chain level 2 per fixture, GPU vs CPU ms).
`cargo test --workspace --release` (whole workspace must pass), `cargo clippy -p pipeline-gpu -p image-core --all-targets -- -D warnings`, fmt.
