# WP M3-06 — Depth estimation, depth masks, lens blur (`ml-depth`)

Read docs/01 §2.16 (Lens Blur), docs/09 §1 (depth) and §2 (lens blur/bokeh, relight placeholder), crates/ml-runtime, crates/ml-segment (registration + caching pattern), crates/pipeline-cpu masks (depth range mask takes a supplied plane), engine-api LensBlur settings and mask depth-range fields.
Licensing: Apache-2.0 weights only — **Depth Anything V2 Small** (Apache-2.0; the Base/Large are CC-BY-NC, do not use); register ONNX export with URL/sha256, document export in tools/.
Implement `crates/ml-depth`: `estimate(image) -> DepthMap` (relative inverse depth, normalised 0–1, cached per image hash + model version, refined to image edges with the guided filter from M2-08); provide the depth plane to pipeline-cpu's depth-range mask; implement the **Lens Blur** operator in pipeline-cpu (Effects stage): focus range from a depth band, blur amount, layered depth-of-field rendering (discretise depth into N layers, blur each with a disc/aperture kernel of size ∝ |depth − focus|, composite back-to-front with mask normalisation to avoid halos), bokeh shape (circle, 6/8-blade, cat-eye toggle placeholder), highlight boost; a GPU port in pipeline-gpu if straightforward (separable approximation acceptable, gate against CPU with tolerance).
Tests: synthetic scene (two flat planes at different intensities with a known depth map) → focus on plane A keeps it sharp (edge contrast unchanged) and blurs plane B (edge contrast drops > 50%); depth range mask selects the far plane; model partition report; fixtures integration (skip offline). Goldens unchanged with lens blur off.
`cargo test -p ml-depth -p pipeline-cpu --release`, clippy -D warnings, fmt. engine-api unchanged.
