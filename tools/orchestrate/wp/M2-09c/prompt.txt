You are implementing work package M2-09c. Work only inside the worktree at /Users/rutmehta/Developer/tessera/.worktrees/M2-09c. Only touch allowed paths matching: Cargo.lock,crates/pipeline-gpu/**,crates/image-core/**,tools/orchestrate/wp/M2-09c/**. IMPORTANT: the repo path contains a colon, which breaks cargo on macOS unless CARGO_TARGET_DIR points outside the repo. It is already exported in your environment as /Users/rutmehta/.cache/tessera-target/M2-09c; keep it set for every cargo command (never build into ./target, never commit target/). Run this test command yourself before finishing: cargo test --workspace --release && cargo clippy -p pipeline-gpu --all-targets -- -D warnings && cargo fmt --check. Finish by printing a line RESULT: PASS or RESULT: FAIL <reason>.

# WP M2-09c — pipeline-gpu geometry parity after the lens package

Main fails: `cargo test -p pipeline-gpu --release --test geometry_compute` → `isolated_geometry_identity_validation_and_extremes` panics at line 109: the CPU Geometry op now *succeeds* on settings the test lists as unsupported, because M2-09 (crates/lens) implemented EXIF orientation, constrain-crop, lens distortion and Upright in pipeline-cpu's Geometry stage (read crates/pipeline-cpu geometry code and crates/lens). The GPU op (crates/pipeline-gpu/src/geometry.rs) still only handles crop + straighten.
Fix: in `GpuStageOp` for `StageId::Geometry`, run the WGSL path only when settings are within what it implements (crop + straighten, no orientation/constrain/lens/upright), otherwise delegate to `CpuStageOp` (readback if resident, document the cost); update `geometry_compute.rs` so "unsupported" cases match the CPU's actual validation (both must agree on error or both succeed with results within the docs/11 §1.3 tolerance). Then, if time allows, port the composed inverse map (lens distortion + upright + transform + crop, Lanczos-3) to WGSL as one kernel and gate it against CPU on synthetic grids and the fixtures.
`cargo test --workspace --release` (whole workspace green), `cargo clippy -p pipeline-gpu --all-targets -- -D warnings`, fmt.
