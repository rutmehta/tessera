You are implementing work package M2-26. Work only inside the worktree at /Users/rutmehta/Developer/tessera/.worktrees/M2-26. Only touch allowed paths matching: Cargo.lock,crates/ml-segment/**,crates/tessera-ffi/src/masks.rs,crates/tessera-ffi/tests/**,apps/mac/**,tools/orchestrate/wp/M2-26/**. IMPORTANT: the repo path contains a colon, which breaks cargo on macOS unless CARGO_TARGET_DIR points outside the repo. It is already exported in your environment as /Users/rutmehta/.cache/tessera-target/M2-26; keep it set for every cargo command (never build into ./target, never commit target/). Run this test command yourself before finishing: cargo test -p ml-segment -p tessera-ffi --release && cargo clippy -p ml-segment -p tessera-ffi --all-targets -- -D warnings && cargo fmt --check && (cd apps/mac && ./build-ffi.sh && swift build && swift test -c release -Xswiftc -enable-testing). Finish by printing a line RESULT: PASS or RESULT: FAIL <reason>.

# WP M2-26 — Brush erase modifier and promptable-mask quality

Two on-screen verification passes reproduced these:
1. **⌥-drag does not erase.** In the loupe brush tool, holding Option while dragging paints another stroke instead of erasing (docs/01 §2.15: Option = erase; the brief M2-14 specified ⌥ erase/subtract). Read apps/mac Loupe mask tools (stroke coalescer, brush cursor, modifier handling) and crates/tessera-ffi/src/masks.rs (begin/add/end stroke with an erase flag). Fix the modifier plumbing (modifier read at mouse-down and on flagsChanged during the drag; the cursor shows an erase state), add a Swift test that a stroke begun with Option carries `erase: true` into the FFI call, and a Rust test that an erase stroke reduces mask coverage.
2. **Promptable masks pick poorly.** Person mask (face box → SAM) spilled over rocks and houses; a click on a house selected a tiny patch. Read crates/ml-segment (MobileSAM decoder use, prompts, refinement) and the Objects/People tools in tessera-ffi masks.rs. Improve: (a) request SAM's multi-mask output and choose by predicted IoU with a containment heuristic (the mask must contain the prompt point/box; prefer the largest mask whose area is < 60% of the image for a point prompt; prefer the mask best matching the box for box prompts); (b) for a click on a large object, add a second pass: derive a box from the first mask and re-prompt with the box; (c) for People, prompt with the face box expanded to a body estimate (width ×3, height ×6 downward, clamped) as a box prompt plus a positive point at the face and negative points at the box corners outside the body estimate; (d) apply guided-filter refinement with the RGB guide at the mask level; (e) expose a "grow/shrink" option. Add tests on synthetic scenes (a disc on a background: point prompt on the disc yields IoU > 0.85 with the disc, not a tiny patch; two adjacent rectangles: a click selects one, not both) and, when the models are cached, an integration test on the Fuji RAF asserting the person/objects masks have plausible area fractions (0.01–0.5).
`cargo test -p ml-segment -p tessera-ffi --release`, clippy -D warnings, fmt, `(cd apps/mac && ./build-ffi.sh && swift build && swift test -c release -Xswiftc -enable-testing)`. Allowed: crates/ml-segment/**, crates/tessera-ffi/src/masks.rs, crates/tessera-ffi/tests/**, apps/mac/**, tools/orchestrate/wp/M2-26/**. Do not edit develop.rs, image-core, pipeline-* (another package is in them).
