# WP M2-24 — Accessibility identifiers and labels on every control

Two computer-use verification passes could not target native sheet controls (Export, Print, Smart Album, Import) or panel controls because they lack accessibility identifiers/labels; M2-13b also noted "the sheet's buttons expose no accessibility titles". Read apps/mac/DESIGN.md (Components), Sources/Tessera/App/Components.swift, all sheets and panels, apps/mac/ACCEPTANCE.md.
Do: give every interactive control a stable `.accessibilityIdentifier` (kebab-case, namespaced by surface: `export.preset`, `export.format`, `print.layout`, `smartalbum.rule`, `masks.brush`, `basic.exposure`, `toolbar.view.grid`, …) and a proper `.accessibilityLabel` where the visible text is an icon; make custom controls (ControlSlider, chips, segmented) expose `.accessibilityValue` and adjustable actions (increment/decrement) so VoiceOver and automation work; ensure sheets have accessible titles. Add a Swift test that walks the SwiftUI view hierarchy of each sheet/panel (via a test harness rendering them) or, simpler, a lint test over Sources/ that every `Button(`, `Toggle(`, `Picker(`, `TextField(`, `ControlSlider(` has an `.accessibilityIdentifier` within the following 3 lines (allow-list exceptions with a comment). Update ACCEPTANCE.md with an "Identifiers" appendix listing the ids per surface so verifiers can target them by name. Keep the Theme lint green.
`(cd apps/mac && ./build-ffi.sh && swift build && swift test -c release -Xswiftc -enable-testing)`. Allowed: apps/mac/**, tools/orchestrate/wp/M2-24/**.
