# WP M2-11 — Albums, smart albums, saved-search grammar, library.json (engine)

Read docs/06 §4 (albums, smart albums scoped to groups, basket, safe delete, derived status), docs/05 §2 (library.json), docs/01 §1.9 (filter bar semantics), crates/index (Query, facets), crates/cull (albums, basket, derived status), crates/import-lrcat (SavedSearch AST + Library writer it defined). Implement `crates/library` (new workspace member) and refactor so there is ONE definition:
- `Library` (library.json): root folders, albums (manual, ordered, ids), album groups (nestable), smart albums (rules), people names, keyword tree, marks preset, publish state placeholder; atomic write; move the struct out of import-lrcat into this crate and make import-lrcat depend on it.
- `SavedSearch` grammar: move the AST from import-lrcat here; add a text form (`rating>=3 AND (keyword:beach OR camera:"Canon") NOT decision:reject date:2024-01..2024-06 lens:85mm focus>0.6 person:"Anna" semantic:"laughing"`), a parser with good errors, a compiler to `index::Query` (facets + FTS + semantic term from ml-embed via a trait so library does not depend on ml-embed), and scope to a group's contents (Capture One Projects behaviour).
- Album operations with safe-delete semantics; derived status hook for cull (`in_album`).
- Tests: parser round trips; compile to Query produces expected SQL/filters over a synthetic index; scoped smart album returns only images inside the group's albums; library.json round trip; importer tests still pass after the move.
`cargo test -p library -p index -p cull -p import-lrcat --release`, clippy -D warnings, fmt. Do not modify engine-api.
