You are implementing work package M2-27. Work only inside the worktree at /Users/rutmehta/Developer/tessera/.worktrees/M2-27. Only touch allowed paths matching: apps/mac/**,tools/orchestrate/wp/M2-27/**. IMPORTANT: the repo path contains a colon, which breaks cargo on macOS unless CARGO_TARGET_DIR points outside the repo. It is already exported in your environment as /Users/rutmehta/.cache/tessera-target/M2-27; keep it set for every cargo command (never build into ./target, never commit target/). Run this test command yourself before finishing: cd apps/mac && ./build-ffi.sh && swift build && swift test -c release -Xswiftc -enable-testing. Finish by printing a line RESULT: PASS or RESULT: FAIL <reason>.

# WP M2-27 — Keyboard focus: sliders and fields must own their keys

On-screen verification: with an HSL slider focused, pressing arrow keys moved the loupe/grid selection instead of nudging the slider (docs/01 §2.2 and M2-13: arrow keys nudge, ⇧ ×10, ⌥ fine; double-click resets). Read apps/mac KeyRouter (app-wide key monitor), ControlSlider (custom NSControl), the curve editor and the inspector panels.
Fix: the key router must yield when the first responder is a ControlSlider, curve editor, text field, or any control that declares it handles keys (a protocol `KeyOwningControl`); ControlSlider becomes focusable (Tab / click) and handles ←/→ (±1 step), ⇧ (×10), ⌥ (fine), Home/End (min/max), Return/Esc (commit/blur), with a visible focus ring from Theme; the histogram/curve editor likewise. Culling keys (X/U/P etc.) never fire while a text field has focus. Add Swift tests: a focused slider receives arrow keys and the router does not move selection; an unfocused loupe still gets arrows; typing in the search field never triggers a cull decision. Update ACCEPTANCE.md step notes for keyboard nudging.
`(cd apps/mac && ./build-ffi.sh && swift build && swift test -c release -Xswiftc -enable-testing)`. Allowed: apps/mac/**, tools/orchestrate/wp/M2-27/**. Theme lint must stay green.
