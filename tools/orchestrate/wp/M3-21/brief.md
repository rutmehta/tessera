# WP M3-21 — Neural filters, batch 1 (skin smoothing, colorize, JPEG artifact removal, photo restoration)

Read docs/02 §5 (Neural Filters row) and docs/13-licensing.md (weights must be Apache-2.0/MIT/BSD; never GPL/non-commercial — CodeFormer and GPEN are NOT acceptable), crates/ml-runtime (registry, models.toml, hash pinning, CoreML partition guard, download-on-demand; tests skip cleanly without weights), crates/ml-enhance (DRUNet denoise + Real-ESRGAN: how a raster→raster model is tiled, padded, run and stitched; training/ conventions), crates/ml-faces (YuNet boxes/landmarks), crates/compositor/src/raster.rs (Raster type used by filters).
Create `crates/ml-filters` (Apache-2.0, workspace member) with a `NeuralFilter` trait (name, params schema, `apply(&Raster, &Params, &Cancel) -> Raster`, `requires_weights()`), plus:
1. **Skin Smoothing** — no model: face-aware frequency separation (low/high split with edge-aware blur), skin mask from face boxes + colour model, controls Blur / Smoothness, preserving texture; deterministic; tests on synthetic face-coloured regions.
2. **Colorize** — DDColor (Apache-2.0) ONNX export (piddnad/DDColor; pin a pre-exported ONNX if its licence is Apache-2.0, otherwise add an export script under crates/ml-filters/training/); controls: colour artefact reduction, saturation, optional focal-point colour hints (blend user hints into the ab output). Registry entry with SHA-256; integration test with the real model skipped when absent.
3. **JPEG Artifact Removal** — reuse the DRUNet denoiser path from ml-enhance with a JPEG-quality-conditioned strength (estimate quality from quantisation tables via the `image`/`zune-jpeg` decoder or from block-boundary energy); controls Strength; test: a synthetic q=30 JPEG improves PSNR vs the clean source.
4. **Photo Restoration** — GFPGAN v1.4 (Apache-2.0, TencentARC) face restoration via ONNX for detected faces (crop, restore at 512, paste back with feathered mask), plus the DRUNet denoise for the whole frame; controls: Photo enhancement, Enhance face, Scratch reduction (documented as not done if no Apache-2.0 model exists). Registry entries pinned; tests skip cleanly without weights.
Register nothing in the compositor yet (another package owns the adapter); expose a `ml_filters::catalog()` listing names/params for later wiring. Update crates/ml-runtime/models.toml with the new entries (no weights committed). Bench (ignored tests, log numbers): each filter on a 12 MP image on CoreML. Gate: `cargo test -p ml-filters -p ml-runtime --release && cargo clippy -p ml-filters -p ml-runtime --all-targets -- -D warnings && cargo fmt --check`. Allowed: Cargo.toml,Cargo.lock,crates/ml-filters/**,crates/ml-runtime/models.toml,crates/ml-runtime/src/registry.rs,crates/ml-runtime/tests/**,crates/ml-enhance/src/lib.rs,tools/orchestrate/wp/M3-21/**.

## Round 2 (scope decision)
Coordinator decision: GFPGAN is **excluded** (StyleGAN2/NVIDIA non-commercial terms in its lineage; your licence read stands). Do not register it; leave `restoration.rs` as denoise-only "Photo Restoration (no face model)" with the reason documented in the crate README and in docs/13-licensing.md's model table. DDColor: the CoreML partition guard rejects its graph; run it explicitly on the CPU execution provider via an opt-in `ExecutionPreference::CpuOnly` on the session (add that option to ml-runtime if missing) with the measured 12 MP time logged; keep the guard strict for other models. Then finish: catalog lists the three shipped filters, all tests green, RESULT: PASS with a precise "not done" list.
