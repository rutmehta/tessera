You are implementing work package M2-29. Work only inside the worktree at /Users/rutmehta/Developer/tessera/.worktrees/M2-29. Only touch allowed paths matching: Cargo.lock,Cargo.toml,crates/image-core/**,crates/pipeline-cpu/**,crates/pipeline-gpu/**,crates/tessera-ffi/src/develop.rs,crates/tessera-ffi/src/export.rs,crates/export/**,crates/previews/**,crates/color-mgmt/**,apps/mac/ACCEPTANCE.md,tools/orchestrate/wp/M2-29/**. IMPORTANT: the repo path contains a colon, which breaks cargo on macOS unless CARGO_TARGET_DIR points outside the repo. It is already exported in your environment as /Volumes/betterSSD/tessera-cache/target/M2-29; keep it set for every cargo command (never build into ./target, never commit target/). Run this test command yourself before finishing: cargo test -p image-core -p pipeline-gpu -p tessera-ffi -p export --release && cargo clippy -p image-core -p pipeline-gpu -p tessera-ffi -p export --all-targets -- -D warnings && cargo fmt --check && (cd apps/mac && ./build-ffi.sh && swift build). Finish by printing a line RESULT: PASS or RESULT: FAIL <reason>.

# WP M2-29 — Develop rendered files (JPEG/TIFF/PNG/HEIC) in the loupe and export

Docs/01 §2: Lightroom develops JPEG/TIFF/PNG/PSD/HEIC too, with the pipeline starting after demosaic (no WB in camera space; Temperature/Tint become relative). Today the develop session refuses non-raws and the loupe shows black for JPEGs (seen on screen). Read crates/image-core (RawImage source, graph: the M1-06 note "RGB input not supported"), crates/pipeline-cpu (render for RGB sources exists?), crates/pipeline-gpu (resident sensor pages are CFA-specific), crates/tessera-ffi/src/develop.rs (open refuses JPEGs), crates/previews (embedded preview decode), crates/color-mgmt (input profile: embedded ICC or sRGB assumed).
Do: an `RgbSource` (decoded via zune-jpeg/png/tiff/ImageIO-equivalent crates with EXIF orientation, ICC → linear Rec.2020 working space through color-mgmt, 8/16-bit and float TIFF) that enters the graph at the post-demosaic point (Demosaic output cache seeded directly; Denoise stage skipped; WB as relative temperature/tint in working space); resident GPU pages for RGB sources; develop session accepts them; export and the agent/MCP paths work on them; recipe records `source_kind`. Tests: a JPEG opens in a develop session and renders non-black; tone edits render within tolerance of pipeline-cpu; embedded ICC (make a synthetic AdobeRGB JPEG in-test) is honoured; 16-bit TIFF preserves precision; HEIC via ImageIO on macOS (feature-gated). Update ACCEPTANCE (loupe on a JPEG).
`cargo test -p image-core -p pipeline-gpu -p tessera-ffi -p export --release`, clippy -D warnings, fmt, `(cd apps/mac && ./build-ffi.sh && swift build)`. engine-api unchanged (report needed fields). Allowed: Cargo.lock, Cargo.toml, crates/image-core/**, crates/pipeline-cpu/**, crates/pipeline-gpu/**, crates/tessera-ffi/src/develop.rs, crates/tessera-ffi/src/export.rs, crates/export/**, crates/previews/**, crates/color-mgmt/**, apps/mac/ACCEPTANCE.md, tools/orchestrate/wp/M2-29/**.
