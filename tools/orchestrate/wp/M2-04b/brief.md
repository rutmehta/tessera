# WP M2-04b — Colour-science fixes, defaults, and native process revision 2

Read crates/pipeline-cpu (OPERATORS.md, color.rs, render.rs, MISSING_FIELDS.md), crates/engine-api CONTRACTS.md invariant 4 (a rendering change bumps `ProcessVersion::NATIVE_CURRENT.revision`), crates/image-core (cache keys include process version), tools/orchestrate/wp/M1-10 notes below.
Fix three things, together, as native revision 2:
1. **White balance model.** As shipped, the as-shot white on all five fixtures sits ~Duv 0.03 off the Planckian locus, so no Temperature/Tint within ±150 reproduces As Shot and the first slider touch jumps. Re-derive: compute the as-shot illuminant chromaticity from the camera multipliers through the camera matrix, then express Temperature as CCT (Robertson or Ohno method) and Tint as Duv (Lightroom's scale: tint ±150 ≈ Duv ±0.05, document the mapping you choose), so that (T_asshot, tint_asshot) is the identity and sliders move smoothly from it. Add tests: as-shot round trip is identity within 1e-4; T+500 K warms (R/B ratio increases) monotonically; tint +50 shifts toward magenta.
2. **Apply the default detail settings.** Sharpening amount 40 / radius 1 / detail 25 and colour NR 25 are Lightroom's defaults for raws and must render by default (remove the "unchanged substructure bypass" hack from M2-04).
3. **Regenerate the goldens** (`fixtures/golden/*.png`) from the new defaults and bump `NATIVE_CURRENT.revision` to 2 in engine-api (this is the one engine-api change allowed here; update the CONTRACTS.md change log). Confirm pipeline-gpu tests still pass against the new CPU reference (its ops delegate for anything unported).
Also enable in `apps/mac` the Basic-panel sliders that were disabled for lack of operators (Texture, Clarity, Dehaze, Vibrance, Saturation now exist since M2-04): remove the disabled state and tooltips; nothing else in Swift.
`cargo test --workspace --release`, clippy -D warnings on pipeline-cpu/pipeline-gpu/image-core/engine-api, fmt, `(cd apps/mac && swift build)`.
