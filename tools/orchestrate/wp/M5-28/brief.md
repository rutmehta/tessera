# WP M5-28 — Compositor follow-ups: Camera Raw filter completeness, GPU Shadows/Highlights, HDR Toning

Read tools/orchestrate/wp/M5-25/RETRY-VERIFICATION.md and crates/filters/README.md (Camera Raw filter contract and its unsupported list), crates/filters/src/camera_raw*.rs, crates/image-core/src/rgb.rs (RgbSource; add an in-memory constructor from a planar f32 raster so the Develop renderer with stage memoisation can be driven by the filter), crates/image-core/src/render.rs + resident_render.rs, tools/orchestrate/wp/M5-26/result.md ("Not done": positive-radius Shadows/Highlights is CPU-only via `render_tile_with_neighbourhood`; HDR Toning not implemented), crates/compositor/src/adjust/shadows.rs, crates/compositor/src/render/exec.rs, crates/compositor/src/resident/** (adjustments.wgsl, program.rs), docs/02 §7 (HDR Toning controls).
1. **Camera Raw filter**: add `RgbSource::from_raster` (in-memory, profile-tagged) in image-core; route the filter's CPU evaluation through the Develop renderer with stage memoisation keyed by (layer revision, settings hash) so slider changes re-run only downstream stages; cover the remaining controls (automatic lens/CA corrections on the GPU, geometry/effects controls listed as unsupported) or document each as intentionally unsupported for RGB layers with a reason. Parity tests vs image-core developing the same pixels.
2. **Shadows/Highlights on the GPU**: implement the local-radius operator as a resident two-pass separable blur of the luminance backdrop with halo-correct tile margins (reuse the smart-filter halo mechanism), bit-exact with the CPU fallback or within 1e-4 with the tolerance documented; remove the Unsupported path for cached rendering; tests at L0/L2 across tile boundaries.
3. **HDR Toning** (Local Adaptation method with radius/strength, gamma, exposure, detail, shadow/highlight, vibrance, saturation, toning curve; plus Equalize Histogram/Exposure-Gamma/Highlight Compression methods) as an adjustment with CPU + GPU, PSD key if one exists (document if native-only).
Gate: `cargo test -p compositor -p filters -p image-core --release && cargo clippy -p compositor -p filters -p image-core --all-targets -- -D warnings && cargo fmt --check`. Allowed: Cargo.lock,crates/compositor/src/**,crates/compositor/tests/**,crates/compositor/COMPOSITOR.md,crates/filters/src/camera_raw.rs,crates/filters/src/camera_raw_gpu.rs,crates/filters/src/compositor_adapter.rs,crates/filters/tests/**,crates/filters/README.md,crates/image-core/src/**,crates/image-core/tests/**,tools/orchestrate/wp/M5-28/**.
