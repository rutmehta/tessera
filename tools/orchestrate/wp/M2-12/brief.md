# WP M2-12 — Library UI: albums, smart albums, filter bar, keywords, metadata (Opus)

Read docs/06 §4 (albums, groups, basket, safe delete), docs/01 §1.6–1.9 (collections, keywording, metadata panel, filter bar; adapt to our model), crates/library (Library, SavedSearch text grammar + parser errors, scoped smart albums), crates/index (facets), crates/tessera-ffi, apps/mac (Sidebar, EngineLibrary, CullController).
1. FFI: expose library operations (albums CRUD with ordering, album groups, smart albums with rule text + parse errors, scope-to-group, basket target), `search(text) -> ids + facets + parse diagnostics`, keyword tree read/write, metadata read (EXIF/IPTC flattened) and IPTC edit for title/caption/copyright/creator/keywords (writes XMP via sidecar).
2. App (AppKit for lists, SwiftUI panels): sidebar with Albums / Album Groups / Smart Albums (rename, reorder, nest via drag; New Smart Album sheet with a rule editor that shows and/or/not nesting visibly plus a free-text field using the grammar with live error highlighting; "scope to this group" toggle); filter bar above the grid (decision, grade, mark, camera, lens, date range, keyword, text; facet counts update live; a "Save as smart album" button); Keywords panel in the inspector with hierarchy and bulk apply to selection; Metadata panel with editable IPTC fields; derived status "not in any album" filter. Safe delete semantics everywhere.
3. ACCEPTANCE.md: a Library section (create album, add via basket, smart album from a filter, scoped smart album inside a group, keyword apply, IPTC edit persists to XMP).
Tests: Rust for the new FFI surface; Swift tests for rule parsing diagnostics through the bridge and album ordering. `cargo test -p tessera-ffi -p library --release`, clippy -D warnings, fmt, `(cd apps/mac && ./build-ffi.sh && swift build && swift test)`. engine-api unchanged.
