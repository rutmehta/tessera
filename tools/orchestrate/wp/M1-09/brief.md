# WP M1-09 — Culling UX on the real engine (Opus)

Read docs/06 §3–4 (the UX spec), crates/cull (merged: CullSession, grouping, keep_best_reject_rest, derived status, defect_sweep), crates/tessera-ffi and apps/mac (merged bridge; app already persists X/U/P and grades via set_selection). Scope:
1. FFI: expose `CullSession` through `crates/tessera-ffi` (open for a folder/query, current position, decide/grade/mark/basket, undo/redo, groups: next/prev/within, keep_best_reject_rest, derived status per image, defect_sweep preview list). Keep pixels out of the bridge. Regenerate Swift bindings with build-ffi.sh.
2. App (AppKit/SwiftUI per docs/11 §1.5): replace the in-memory `CullState` with the FFI session so undo/redo and groups come from Rust; group-by-group navigation (←/→ groups, ↑/↓ within, ⌥-arrows in grid), the group's suggested best pre-highlighted, one key (⌘K or as you judge) keeps best and rejects the rest with an undoable toast; Basket target shown persistently; derived status badge (unedited/edited/exported/in album) on cells; "Defect sweep" sheet that lists candidates from `defect_sweep` with checkboxes and applies on confirm (undoable); Compare view: 2-up with synced zoom/pan and a "choose this" key (loupe zoom via the existing Metal view; a simple CGImage-based 2-up is fine for now). Safe delete semantics per docs/06 §4.2 (delete in album removes from album only; "Delete from disk" separate and confirmed).
3. ACCEPTANCE.md update for a computer-use verifier: numbered steps over fixtures/raw exercising group nav, keep-best, undo, defect sweep (with synthetic scores injected via a hidden `--seed-scores` flag), compare.
Design bar: Capture One / Pixelmator Pro restraint; keyboard-first; no modal blocking. Tests: Rust tests for the new FFI surface; Swift tests for group navigation and keep-best via the bridge. `cargo test -p tessera-ffi -p cull --release`, clippy -D warnings, fmt, `(cd apps/mac && swift build && swift test)`.
