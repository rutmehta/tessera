You are implementing work package M3-05. Work only inside the worktree at /Users/rutmehta/Developer/tessera/.worktrees/M3-05. Only touch allowed paths matching: Cargo.lock,Cargo.toml,crates/ml-enhance/**,crates/ml-runtime/models.toml,crates/pipeline-cpu/**,crates/image-core/**,crates/export/**,tools/**,tools/orchestrate/wp/M3-05/**. IMPORTANT: the repo path contains a colon, which breaks cargo on macOS unless CARGO_TARGET_DIR points outside the repo. It is already exported in your environment as /Users/rutmehta/.cache/tessera-target/M3-05; keep it set for every cargo command (never build into ./target, never commit target/). Run this test command yourself before finishing: cargo test -p ml-enhance -p pipeline-cpu -p image-core -p export --release && cargo clippy -p ml-enhance -p pipeline-cpu -p image-core -p export --all-targets -- -D warnings && cargo fmt --check. Finish by printing a line RESULT: PASS or RESULT: FAIL <reason>.

# WP M3-05 — AI denoise, raw details, super-resolution (`ml-enhance`)

Read docs/01 §2.9, docs/07 §2 (joint denoise+demosaic, capture sharpening) and §7, docs/09 §2, crates/ml-runtime, crates/ml-embed (registration pattern), crates/pipeline-cpu (Denoise stage placeholder in StageId order; demosaic ops), crates/image-core (cacheable Denoise stage), engine-api enhance/denoise settings fields.
Licensing (docs/13): Apache-2.0/MIT/BSD weights only. Research current permissive options and pick (document licence + URL + sha256 in models.toml; never commit weights): denoise — **NAFNet** (MIT, SIDD weights) or **Restormer** (check licence) on linear RGB after demosaic as phase 1 (true joint CFA denoise needs training: leave a `CfaDenoise` trait and a TODO); super-resolution — **Real-ESRGAN x2/x4** (BSD-3) or **SwinIR** (Apache-2.0); document why.
Implement `crates/ml-enhance`: `denoise(linear_rgb, amount, noise_model_hint)` with tiled inference (halo ≥ receptive field), amount blends net output with input, optional mask (local application, uses M2-08 mask rasters); `super_resolution(rgb, factor)` tiled; both run via ml-runtime on CoreML with fp16 where accurate. Wire denoise into the Denoise stage in pipeline-cpu/image-core (cacheable; results keyed by model version) behind the enhance settings; SR as an export-time option in `crates/export` (`--upscale 2|4`).
Tests: synthetic noisy gradient → PSNR improves by ≥ 3 dB after denoise (skip if model not cached); amount 0 is identity bit-exact; tiling seams: full vs tiled within 1e-4; SR doubles dimensions and preserves a sharp edge; CoreML partition report; goldens unchanged with enhance off.
`cargo test -p ml-enhance -p pipeline-cpu -p image-core -p export --release`, clippy -D warnings, fmt. engine-api unchanged (report needed fields).
