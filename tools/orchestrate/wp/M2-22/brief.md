# WP M2-22 — HDR/EDR presentation for raws (Opus)

Read docs/07 §6 (display: EDR, P3/Rec.2020 PQ, 10-bit, headroom slider), docs/01 §2.19 (HDR editing), docs/11 §1.2 (Swift owns presentation), crates/tessera-ffi/src/surface.rs and develop.rs (RGBA8 IOSurface ring, display-encoded sRGB written by Rust), crates/pipeline-gpu (managed output, resident presentation from M2-15/M2-17b), crates/color-mgmt (display transforms), apps/mac Loupe (CAMetalLayer with wantsExtendedDynamicRangeContent, colour space from screen), the soft-proof LUT path from M2-20.
Goal: the loupe shows scene-referred headroom on EDR displays: engine writes **RGBA16Float linear** (extended sRGB or Display P3 linear) into the IOSurface, Swift presents it on a `.rgba16Float` layer with `colorspace = extended linear` and the screen's `maximumExtendedDynamicRangeColorComponentValue` used as headroom; a Develop "HDR" toggle + headroom slider (0 → SDR tone-mapped, up to the display's potential) bound to the recipe's HDR settings (fields exist: check engine-api; if not, report and keep it session-only); SDR displays fall back to the current path automatically; the output stage's tone mapper keeps SDR renditions identical to today's goldens.
Keep the < 12 ms slider budget (M2-17b) on the float path. Tests: Rust for the float surface writer (values > 1.0 survive), SDR fallback selection, and that SDR output is bit-identical to before; Swift test for headroom computation from a mocked screen; ACCEPTANCE.md section with an EDR-capable-display note (the verifier reports the screen's headroom).
`cargo test -p tessera-ffi -p pipeline-gpu -p color-mgmt --release`, clippy, fmt, `(cd apps/mac && ./build-ffi.sh && swift build && swift test)`. engine-api unchanged (report needed fields). Another package (M2-18b) is making a minimal process-version change in develop.rs; keep your develop.rs edits scoped to surface format/headroom.
