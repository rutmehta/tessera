You are implementing work package M2-04. Work only inside the worktree at /Users/rutmehta/Developer/tessera/.worktrees/M2-04. Only touch allowed paths matching: Cargo.lock,Cargo.toml,crates/pipeline-cpu/**,crates/image-core/**,tools/orchestrate/wp/M2-04/**. IMPORTANT: the repo path contains a colon, which breaks cargo on macOS unless CARGO_TARGET_DIR points outside the repo. It is already exported in your environment as /Users/rutmehta/.cache/tessera-target/M2-04; keep it set for every cargo command (never build into ./target, never commit target/). Run this test command yourself before finishing: cargo test -p pipeline-cpu -p image-core --release && cargo clippy -p pipeline-cpu -p image-core --all-targets -- -D warnings && cargo fmt --check. Finish by printing a line RESULT: PASS or RESULT: FAIL <reason>.

# WP M2-04 — Milestone 2 operators, CPU reference (`pipeline-cpu`)

Read docs/01 §2.3–2.7, §2.11–2.12, docs/07 §5, crates/pipeline-cpu (OPERATORS.md, render.rs, the StageId order and DevelopSettings fields in crates/engine-api/src/recipe/settings.rs), crates/image-core (StageOp, graph cacheability). Implement, as scalar f32 reference operators wired into `pipeline_cpu::render*` and the per-stage `StageOp` calls, with formulas documented in OPERATORS.md and unit tests each:
- **Tone stage additions**: Texture (guided-filter based local contrast at fine scale), Clarity (local Laplacian pyramid or guided filter at mid scale, halo-free), Dehaze (dark-channel + guided transmission, airlight estimate robust to bright scenes), parametric tone curve (highlights/lights/darks/shadows with split points) and point curves (monotone cubic, luminance and per-channel RGB) on a log-encoded axis.
- **Color stage**: Vibrance and Saturation in OkLCh, HSL (8 hue bands: hue/sat/lum with smooth band weights) in OkLCh, Color Grading (shadows/midtones/highlights/global wheels with blending and balance), B&W mix.
- **Detail stage**: capture sharpening (unsharp mask with radius/detail/masking as Lightroom exposes; deconvolution later), luminance and chroma noise reduction (bilateral/guided on luminance; chroma smoothing in OkLab) as the manual (non-AI) path.
- **Effects stage**: post-crop vignette (highlight priority + color priority + paint overlay styles), grain (size/roughness, deterministic seed).
- **Geometry stage**: crop rectangle + straighten angle composed into the single inverse map with Lanczos-3 sampling (Upright/lens distortion later); output dimensions respect the crop.
Every operator must be neutral at its default (test: default settings render bit-identical to the current goldens; do NOT change goldens), monotone where applicable, and free of NaN on clipped input (test with the fixtures at level 3 with extreme settings). Keep the public render API stable; add fields only where DevelopSettings already has them (engine-api unchanged; if a needed field is missing, list it in `MISSING_FIELDS.md` and skip that control).
`cargo test -p pipeline-cpu -p image-core --release`, clippy -D warnings, fmt.
