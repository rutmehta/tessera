You are implementing work package M2-14b. Work only inside the worktree at /Users/rutmehta/Developer/tessera/.worktrees/M2-14b. Only touch allowed paths matching: Cargo.lock,Cargo.toml,crates/export/**,crates/tessera-ffi/src/preview.rs,crates/tessera-ffi/src/masks.rs,crates/tessera-ffi/src/lib.rs,crates/image-core/src/mask_cache.rs,crates/mask-ai/**,apps/mac/**,tools/orchestrate/wp/M2-14b/**. IMPORTANT: the repo path contains a colon, which breaks cargo on macOS unless CARGO_TARGET_DIR points outside the repo. It is already exported in your environment as /Users/rutmehta/.cache/tessera-target/M2-14b; keep it set for every cargo command (never build into ./target, never commit target/). Run this test command yourself before finishing: cargo test -p export -p tessera-ffi -p image-core --release && cargo clippy -p export -p tessera-ffi -p image-core --all-targets -- -D warnings && cargo fmt --check && (cd apps/mac && ./build-ffi.sh && swift build && swift test). Finish by printing a line RESULT: PASS or RESULT: FAIL <reason>.

# WP M2-14b — Flaky Swift tests + AI masks in export

1. Two Swift tests fail intermittently under load (seen by the verifier and by M2-13c): `BridgeTests.testRawFixturesPersistAcrossReopenThroughTheSession` (assertion at BridgeTests.swift:50 about the thumbnail cache) and `PreviewEventsTests.testCacheDoesNotAliasReusedDenseItemIDs`. Read them, crates/tessera-ffi/src/preview.rs and the Swift ThumbnailLoader/EngineLibrary preview-event path. Find the real race (likely preview-ready callbacks racing with cache keys / reused dense ids) and fix it in the engine or app code, not by loosening the assertions; make the tests deterministic (await the specific callback). Run `swift test` 10 times in a loop under `RUST_TEST_THREADS=1 cargo test --workspace --release` load to prove stability.
2. Export cannot render recipes containing AI masks because only develop sessions install `image_core::MaskHooks` (M2-14). Make `crates/export` install the same hooks (via the ml-segment-backed implementation that tessera-ffi uses; move the shared implementation into a small crate or into image-core behind a feature so export and tessera-ffi share it), with a test that exports a recipe with a Subject mask + local exposure (fake segmenter in test) and the output differs from the export without the mask.
`cargo test -p export -p tessera-ffi -p image-core --release`, clippy -D warnings, fmt, `(cd apps/mac && ./build-ffi.sh && swift build && swift test)`. Allowed: Cargo.lock, Cargo.toml, crates/export/**, crates/tessera-ffi/src/preview.rs, crates/tessera-ffi/src/masks.rs, crates/tessera-ffi/src/lib.rs, crates/image-core/src/mask_cache.rs, crates/mask-ai/** (new, if you split), apps/mac/**, tools/orchestrate/wp/M2-14b/**.
