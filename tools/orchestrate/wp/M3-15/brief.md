# WP M3-15 — Keywords, captions and OCR in the app (Opus)

Read docs/09 §3 (keywording: suggestions with confidence, bulk accept, hierarchical mapping) and §1 (OCR: search inside images), crates/ml-caption (README, API, jobs, XMP opt-in), crates/library (keyword tree), crates/index (FTS incl. OCR text), crates/tessera-ffi (collections.rs keywords/metadata, assist.rs job patterns), apps/mac (DESIGN.md/Components; Keywords + Metadata panels from M2-12; Identifiers appendix).
1. FFI: `suggest_keywords(image_ids)` (batched job with progress; results cached in the index), `caption(image_ids)`, `ocr(image_ids)`, accept/reject suggestions (writes keywords through the existing keyword path and XMP opt-in), settings for "auto-suggest on import" and "write suggested keywords to XMP".
2. App: Keywords panel gains a **Suggested** section (chips with confidence bars, click to accept, ⇧-click to accept all above a threshold slider, ✕ to reject; mapped-under-tree indicator), a "Suggest for selection" action and the auto-suggest preference (Settings ▸ AI); Metadata panel gains Caption and Alt text fields with a "Generate" button (editable before save) and an OCR "Text in image" read-only block; the filter bar's search matches OCR text and captions (a "text:" term in the grammar if not present — coordinate with crates/library's parser, additive); background jobs show in the status strip. Everything via Theme/Components; identifiers appended to ACCEPTANCE.md with a new section.
Tests: Rust for the FFI surface with a fake captioner; Swift for the suggestion chip model (accept/reject/threshold) and search-term serialisation. `cargo test -p tessera-ffi -p ml-caption -p library --release`, clippy, fmt, `(cd apps/mac && ./build-ffi.sh && swift build && swift test -c release -Xswiftc -enable-testing)`. engine-api unchanged. Do not edit crates/tessera-ffi/src/tether.rs or agent_runs.rs (other packages).
