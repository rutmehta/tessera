# WP M3-11 — Assisted culling UI and agent review UI (Opus)

Read docs/06 §3 (face strip, defect sweep, assisted vs automated, learning), docs/10 §2 ("Fine-tune surface": Agent base edit history group with one amount slider, per-step toggles, explainability, NL redo; review queue by confidence), crates/cull (M3-07: learner, modes, face_strip, best-of-burst), crates/style-profile (batch apply, review queue, feedback), crates/agent (planner/critic, redo, providers, Console), crates/tessera-ffi (session.rs, masks.rs, lrcat.rs for job/progress patterns), apps/mac (DESIGN.md — use Theme and Components only; the Theme lint will fail otherwise; ACCEPTANCE.md Identifiers appendix; Components.swift).
1. FFI: expose assisted culling (mode assisted/automated, predictions with p_keep + explanation, queue reordering, suggested decisions with confirm/reject-all, learner feedback), face strip per image (chips with crop rects, focus, eyes-open, person id), per-person filter; style-profile: train status, questionnaire, batch apply job with progress and review queue; agent: run base edit (single/batch) with provider selection and progress, redo with instruction, provenance summary.
2. App:
   - **Culling**: face strip below the loupe (chips with green/yellow/red focus and eyes badges, click to zoom), assisted mode toggle in the toolbar with a confidence sort, "suggested" decisions shown as translucent pills with a one-key confirm-all and per-image reject; defect sweep uses real scores now (remove the seed flag dependence).
   - **Agent**: an "Auto edit" command (toolbar + ⌘⇧A) with a sheet: provider (style profile only / Anthropic / OpenAI / Ollama, keys read from Keychain via a Settings pane you add: Settings ▸ AI with secure fields; never log keys), scope (selection/album/shoot), consistency options, guardrails; progress strip; review queue view sorted by confidence with accept/redo/revert per image; in Develop, the History panel shows the "Agent base edit" group with a fade slider (0–100%) and per-step toggles plus the rationale strings; a "Redo with instruction…" field (NL redo) on the group.
   - Preferences persist under app-dir; keys in Keychain.
3. ACCEPTANCE.md sections for assisted culling and agent review (use FakePlanner via a hidden flag so the verifier needs no key), identifiers appended.
Tests: Rust for the FFI surface (fake planner/scorers); Swift for the review-queue model, fade slider patch generation, and Keychain wrapper (mocked). `cargo test -p tessera-ffi -p cull -p style-profile -p agent --release`, clippy, fmt, `(cd apps/mac && ./build-ffi.sh && swift build && swift test -c release -Xswiftc -enable-testing)`. engine-api unchanged (report needed fields, e.g. group amount). Do not edit crates/image-core, pipeline-*, or crates/export (another package is in them).
