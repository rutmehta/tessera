# WP M3-14 — Agent runs on the GPU renderer at preview resolution

M3-11 measured agent runs at ~3 s per JPEG and ~40 s per 36 MP NEF because `tessera-mcp`'s Console renders the full-resolution image on the CPU at every plan step (critic metrics + preview). Read crates/tessera-mcp (Console, render/compare/histogram tools), crates/agent (loop, critic), crates/image-core (Renderer, resident GPU path, levels), crates/export (GPU path), crates/tessera-ffi/src/agent_runs.rs.
Do: the Console renders through `image_core::Renderer` (GPU when available, CPU fallback) at a **preview level** (long edge ≤ 1024 px, level chosen from the pyramid) for critic metrics and previews, with one cached decode per image across the whole loop and stage memoisation reused across steps (only the changed stage re-runs); `compare` and `get_histogram` use the same cached renderer; full-resolution render only for the final accept if the caller asks. Critic metrics documented as preview-resolution (clipping %, mean luminance, skin ΔE on face boxes scaled). Target: ≤ 0.3 s per step on the NEF at preview level on the M4; a whole 3-step plan ≤ 1.5 s per image. Bench (ignored, prints) before/after on the five fixtures with the FakePlanner. Tests: metrics at preview level agree with full-res metrics within 2% on the fixtures; stage memoisation reuse verified by counting operator invocations across steps; agent tests still pass.
`cargo test -p tessera-mcp -p agent -p tessera-ffi --release`, clippy -D warnings, fmt. engine-api unchanged. Allowed: Cargo.lock, Cargo.toml, crates/tessera-mcp/**, crates/agent/**, crates/tessera-ffi/src/agent_runs.rs, tools/orchestrate/wp/M3-14/**.
