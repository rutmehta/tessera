You are implementing work package M5-13. Work only inside the worktree at /Users/rutmehta/Developer/tessera/.worktrees/M5-13. Only touch allowed paths matching: Cargo.lock,Cargo.toml,crates/vector/**,tools/orchestrate/wp/M5-13/**. IMPORTANT: the repo path contains a colon, which breaks cargo on macOS unless CARGO_TARGET_DIR points outside the repo. It is already exported in your environment as /Volumes/betterSSD/tessera-cache/target/M5-13; keep it set for every cargo command (never build into ./target, never commit target/). Run this test command yourself before finishing: cargo test -p vector --release && cargo clippy -p vector --all-targets -- -D warnings && cargo fmt --check. Finish by printing a line RESULT: PASS or RESULT: FAIL <reason>.

# WP M5-13 — Vector paths and shape layers (`vector`)

Read docs/02 §1.5 (paths, vector masks), §9 (vector shapes: rectangle/rounded/ellipse/polygon/line/custom shape, live shape properties, stroke options: width/alignment/dashes/caps/joins, fill: solid/gradient/pattern, path operations: combine/subtract/intersect/exclude, pen/curvature pen), §6 (transform: free transform, warp, perspective, content-aware scale placeholder), crates/compositor (vector mask placeholder; do not edit compositor; expose a `VectorRenderer` producing coverage/RGBA rasters), crates/psd (vmsk/vsms path records, shape layer keys), engine-api.
Implement `crates/vector` with permissive deps (`lyon` for tessellation/stroking, `kurbo` for geometry; check licences): path model (subpaths of cubic Béziers, closed/open, even-odd/nonzero), shape primitives with live properties (corner radii per corner, star/polygon params), boolean operations (robust: implement via `lyon`'s or a scanline approach; document limits), stroking (width, alignment inside/center/outside, dashes, caps, joins, miter limit), fills (solid, linear/radial/angle/reflected/diamond gradients with dithering, pattern tiles), anti-aliased rasterisation to coverage masks and RGBA at any level with exact analytic coverage where feasible (or 16× supersampling documented), transforms (affine + perspective + mesh warp with Bézier handles), hit-testing and anchor editing helpers for the UI, PSD path-record interop (read/write the 26-byte records), SVG import/export of paths (basic).
Tests: coverage of a unit circle vs analytic area within 0.1%; stroke width and alignment produce expected bounds; dash pattern lengths; boolean ops on overlapping rects give exact areas; gradient endpoints exact; perspective warp maps corners exactly; PSD path record round trip; SVG round trip on a simple path.
`cargo test -p vector --release`, clippy -D warnings, fmt. engine-api unchanged (write NEEDS.md). Allowed: Cargo.lock, Cargo.toml, crates/vector/**, tools/orchestrate/wp/M5-13/**.
