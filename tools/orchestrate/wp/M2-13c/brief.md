# WP M2-13c — Fix Swift strict-concurrency error in DevelopPanelsTests on CI

GitHub CI (macOS runner, `swift build -c release && swift test -c release`) fails: `apps/mac/Tests/TesseraCoreTests/DevelopPanelsTests.swift:290:32: error: passing closure as a 'sending' parameter risks causing data races between main actor-isolated code and concurrent execution of the closure`. Locally (Swift 6.3.3) it builds. Make the tests correct under Swift 6 strict concurrency for both toolchains: isolate the closure properly (e.g. mark the test `@MainActor`, capture `Sendable` values only, or use `MainActor.assumeIsolated` / `nonisolated(unsafe)` only where justified with a comment). Also check `swift build -c release && swift test -c release` locally (release config, as CI runs it), plus the debug config. Run `xcrun swift --version` and record it. Only touch apps/mac/Tests/**, apps/mac/Sources/** if strictly needed, and tools/orchestrate/wp/M2-13c/**.
`(cd apps/mac && ./build-ffi.sh && swift build -c release && swift test -c release && swift test)`.
