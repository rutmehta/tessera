You are implementing work package M5-04. Work only inside the worktree at /Users/rutmehta/Developer/tessera/.worktrees/M5-04. Only touch allowed paths matching: Cargo.lock,Cargo.toml,crates/compositor/**,crates/psd/**,crates/gpu-core/**,crates/pipeline-gpu/**,tools/orchestrate/wp/M5-04/**. IMPORTANT: the repo path contains a colon, which breaks cargo on macOS unless CARGO_TARGET_DIR points outside the repo. It is already exported in your environment as /Users/rutmehta/.cache/tessera-target/M5-04; keep it set for every cargo command (never build into ./target, never commit target/). Run this test command yourself before finishing: cargo test -p compositor -p psd --release && cargo clippy -p compositor -p psd --all-targets -- -D warnings && cargo fmt --check. Finish by printing a line RESULT: PASS or RESULT: FAIL <reason>.

# WP M5-04 — PSD ↔ compositor adapter and GPU-resident compositing

Read crates/compositor (COMPOSITOR.md, document.rs, gpu.rs, render/*), crates/psd (PsdDocument IR, keys), crates/pipeline-gpu (resident model, batch, buffer reuse patterns from M2-17b/M2-21c).
1. **Adapter** `crates/compositor/src/psd.rs` (or a small `compositor-psd` crate): `Document::from_psd(PsdDocument)` and `to_psd(&Document)` covering pixel layers (8/16/32-bit, channels, masks), groups (pass-through/isolated), blend modes (map every PSD key; document unmatched), opacity/fill, clipping, Blend If ranges, adjustment layers that both sides support, smart objects (embedded originals as nested documents), text as placeholder with preserved engine data, unknown keys preserved opaquely for round trip. Tests: round trip PSD → Document → PSD preserves layer tree and pixels; composite of an imported multi-layer PSD (built in-test) matches the compositor's own render of the equivalent Document.
2. **GPU-resident compositing**: replace the per-tile upload model in compositor/gpu.rs with resident layer textures (uploaded once per revision, mip chains on the GPU), a persistent step buffer, and a whole-level composite per frame; adjustment layers on GPU for the implemented set; dirty-rect updates on GPU. Reuse pipeline-gpu's device via a shared `GpuContext` feature (avoid the separate Metal device if it can be done without pulling LibRaw into compositor: a `gpu-shared` feature or a tiny `gpu-core` crate split out of pipeline-gpu holding only the device/queue/utilities). Targets: 100-layer 20 MP document, level 2 composite < 16 ms after a brush dab; full L0 composite < 100 ms. Gate vs CPU per docs/11 §1.3.
`cargo test -p compositor -p psd --release` (+ the new crate if split), clippy -D warnings, fmt. engine-api unchanged (M5-03 is revising it concurrently: code against the current API; ids are additive). Allowed: Cargo.lock, Cargo.toml, crates/compositor/**, crates/psd/**, crates/gpu-core/**, crates/pipeline-gpu/**, tools/orchestrate/wp/M5-04/**.
