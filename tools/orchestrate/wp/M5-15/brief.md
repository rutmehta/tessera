# WP M5-15 — Alpha channels and spot channels in the document; PSD interop

Read docs/02 §1.5 (channels: alpha channels, spot channels, channel masks), crates/compositor (document.rs: no channel slot; selections as rasters), crates/selection (saved selections stored in the crate, not the document — M5-05/M5-10 notes), crates/psd (channel data, alpha channel names `unam`, spot colours), crates/tessera-mcp (saved selections outside history).
Do: add named alpha channels (and spot channels with colour/solidity) to `compositor::Document` with history-tracked ops (add/delete/rename/edit), storage in `.tessera-doc`, PSD read/write of alpha and spot channels, `selection` saves/loads through the document, and tessera-mcp's saved selections become document channels so undo removes them. Tests: round trips (.tessera-doc and PSD), history undo/redo on channels, selection save/load equality, spot channel rendering placeholder (composite unaffected; documented).
`cargo test -p compositor -p psd -p selection -p tessera-mcp --release`, clippy -D warnings, fmt. engine-api unchanged (report needed fields). Allowed: Cargo.lock, Cargo.toml, crates/compositor/src/document.rs, crates/compositor/src/edit.rs, crates/compositor/src/format.rs, crates/compositor/src/psd.rs, crates/compositor/src/channels.rs, crates/compositor/tests/**, crates/psd/**, crates/selection/**, crates/tessera-mcp/**, tools/orchestrate/wp/M5-15/**. Do not touch crates/compositor/src/resident/** or gpu.rs (another package).
