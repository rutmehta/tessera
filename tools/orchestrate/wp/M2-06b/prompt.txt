You are implementing work package M2-06b. Work only inside the worktree at /Users/rutmehta/Developer/tessera/.worktrees/M2-06b. Only touch allowed paths matching: Cargo.lock,crates/image-core/**,crates/pipeline-gpu/**,crates/pipeline-cpu/**,crates/tessera-ffi/**,tools/orchestrate/wp/M2-06/**,tools/orchestrate/wp/M2-06b/**. IMPORTANT: the repo path contains a colon, which breaks cargo on macOS unless CARGO_TARGET_DIR points outside the repo. It is already exported in your environment as /Users/rutmehta/.cache/tessera-target/M2-06b; keep it set for every cargo command (never build into ./target, never commit target/). Run this test command yourself before finishing: cargo test --workspace --release && cargo clippy -p image-core -p pipeline-gpu -p tessera-ffi --all-targets -- -D warnings && cargo fmt --check. Finish by printing a line RESULT: PASS or RESULT: FAIL <reason>.

# WP M2-06b — Reconcile the GPU-resident model with native revision 2 defaults

Main currently fails: `cargo test -p image-core --release` → `resident_model::resident_graph_preserves_crop_phase_parity_and_edit_invalidation` and `resident_model::preview_matrices_run_only_at_output_resolution` panic at crates/image-core/src/resident_model.rs:41. Cause: M2-06 (GPU-resident render path) and M2-04b (default sharpening/NR now applied, native process revision 2, new WB model) were developed in parallel. Read both packages' notes (tools/orchestrate/wp/M2-06/validation.md, tools/orchestrate/wp/M2-04b/RESULT.md), crates/image-core/src/resident_model.rs, graph.rs, crates/pipeline-gpu/src/batch.rs.
Fix so the resident model's stage/level assumptions hold with the revision-2 defaults (detail stage active by default at output resolution; WB matrices; crop phase parity), without weakening the tests' intent. Then run the whole workspace and the ignored GPU bench and confirm the M2-06 numbers still hold (tone-only ≤ 5 ms, WB ≤ 40 ms at level 2 on the NEF); record them in validation.md.
`cargo test --workspace --release`, `cargo clippy -p image-core -p pipeline-gpu -p tessera-ffi --all-targets -- -D warnings`, fmt.
