# WP M1-03 — sidecar crate: XMP + recipe JSON round trip

Read crates/engine-api/CONTRACTS.md first and use its types; do not modify engine-api (if a contract change is unavoidable, stop and print RESULT: FAIL with the reason so it can be reviewed). The workspace root Cargo.toml lists this crate already; add dependencies via [workspace.dependencies] where the version is pinned there. Everything must pass `cargo test -p <crate>` and `cargo clippy -p <crate> --all-targets -- -D warnings` and `cargo fmt --check`. Fixtures: `fixtures/raw/` has one CR3, ARW, NEF, RAF, DNG each (run `bash fixtures/fetch.sh` if missing). Tests that need fixtures must skip with a message when the directory is absent.

Implement `crates/sidecar` per docs/05 §2 and docs/06 §2.1:
- `Sidecar::paths(image_path) -> {xmp: <image>.<ext>.xmp, recipe: .edits/<stem>.json}`.
- Recipe JSON: read/write `engine_api::recipe::Recipe` with atomic write (temp + rename), a `vector_clock` map (machine_id → counter) alongside, and last-writer-wins merge helper.
- XMP sidecar: read/write a standard XMP packet with `quick-xml`: `xmp:Rating`, `xmp:Label`, `dc:subject` keyword bag, `lr:hierarchicalSubject`, IPTC core fields (title, description, creator, copyright), and the `crs:` develop keys from `engine_api::recipe::crs::CrsKey` (write every key that has a recipe_path; read every key in the table). Preserve unknown XMP properties on round trip (keep the raw XML of foreign namespaces).
- Selection mapping exactly per docs/06 §2.1 table (Reject → Rating -1 + `lr:... ` reject flag as Lightroom writes it: research the actual property, it is not standardised; document what you chose), Keep → 1★ + pick, grades → 2/3/5★, marks → Label text via a `MarkPreset` (Lightroom names Red/Yellow/Green/Blue/Purple by default).
- Tests: recipe round trip is byte-identical after two writes; XMP round trip preserves an unrelated foreign namespace; selection → XMP → selection is identity for all combinations; a fixture XMP written by Lightroom Classic (author one in tests from Adobe's documented format for a PV6 image with Exposure2012, ToneCurvePV2012, a MaskGroupBasedCorrections block) parses and maps to the expected recipe fields. Flag in a `FINDINGS.md` any `CrsKey` names you believe are wrong (the contracts author was unsure about Enhance*, HDREditMode, HDRMaxValue, LensProfileSetup, PostCropVignetteStyle) with evidence from Adobe's XMP documentation.
