You are implementing work package M2-02. Work only inside the worktree at /Users/rutmehta/Developer/tessera/.worktrees/M2-02. Only touch allowed paths matching: Cargo.lock,Cargo.toml,crates/sidecar/**,crates/import-lrcat/**,tools/orchestrate/wp/M2-02/**. IMPORTANT: the repo path contains a colon, which breaks cargo on macOS unless CARGO_TARGET_DIR points outside the repo. It is already exported in your environment as /Users/rutmehta/.cache/tessera-target/M2-02; keep it set for every cargo command (never build into ./target, never commit target/). Run this test command yourself before finishing: cargo test -p sidecar -p import-lrcat --release && cargo clippy -p sidecar -p import-lrcat --all-targets -- -D warnings && cargo fmt --check. Finish by printing a line RESULT: PASS or RESULT: FAIL <reason>.

# WP M2-02 — Sidecar develop export completeness

Read crates/sidecar (src/develop.rs, tests), tools/orchestrate/wp/M1-03/FINDINGS.md, crates/engine-api CONTRACTS.md 1.1 (crs table with namespaces), crates/import-lrcat/src/xmp.rs (its crs text parser; share code if sensible by moving a common parser into sidecar and depending on it from import-lrcat).
Close the gaps listed in FINDINGS.md: export/import of nonempty `PointColors`, `LensBlur`, `RetouchAreas`/`RetouchInfo`, and all `MaskGroupBasedCorrections` mask kinds the recipe model supports (linear + radial gradients, brush stroke references, luminance/colour range masks, AI mask placeholders with model refs), `Look` tables, per-channel tone curves, `ColorGrade*`, `HueAdjustment*`/`SaturationAdjustment*`/`LuminanceAdjustment*`, `Grain*`, `PostCropVignette*`, `Upright*`/`Perspective*`, `Crop*`. Every recipe field with a crs path must round-trip: write a property-based test that generates random `DevelopSettings` (proptest), exports to XMP, imports, and asserts equality of the mapped subset; unmapped fields are listed in `UNMAPPED.md` with a reason.
Also fix the two gaps noted by the contract revision: read recipes through `Recipe::from_json` so schema upgrades apply, and only honour `ts:NativeRevision` when the packet's `crs:` values hash-match our last export (store `ts:ExportHash`).
`cargo test -p sidecar -p import-lrcat --release`, clippy -D warnings, fmt. Do not modify engine-api (report if needed).
