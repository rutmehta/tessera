# WP M5-07 — Document tool executor: MCP + console for layered documents (Opus)

Read crates/engine-api 1.2 (`DocumentToolCall`, `DocumentHistory`, `Action` registry, CONTRACTS.md invariants 16–18), crates/tessera-mcp (Console, tool list generation, schemas, history authoring), crates/compositor (Document, edits, history, resident renderer, format), crates/psd (adapter), crates/filters, crates/brush and crates/selection if merged (M5-05 is in progress: feature-gate their use behind `cfg` or a trait so this compiles without them), docs/02 §11 (automation: Actions, batch, droplets), docs/10.
Implement in tessera-mcp: a `DocumentSession` registry keyed by `DocumentId` (open from `.tessera-doc`/PSD/flat image, save/export), handlers for every `DocumentToolCall` (open_document, add_layer, set_layer_props, paint_stroke via brush when available else a simple round dab, set_pixel_selection via selection tools when available else marquee/wand basics, apply_adjustment_layer, transform_layer, merge_down, export_document, list_layers) each producing exactly one `DocumentHistoryEntry` with `Author::Agent` and the request's rationale (invariant 11/17), `expect_head` checks, previews via the resident renderer at a preview level, and `describe_document` (layers, sizes, blend modes, thumbnails). Publish them in `tools/list` (unique names across both enums); JSON schemas from serde. **Actions**: record every executed tool call as an `Action`, `actions/record|stop|play` tools that replay a recorded sequence on another document/batch (docs/02 §11), with a `.tessera-action` JSON file format; `tessera actions play <file> <inputs…>` in the CLI. Tests: end-to-end over the in-memory transport (open flat image → add adjustment layer → paint → export → history entries with rationale); expect_head conflict → error; record/play reproduces identical pixels on a second document; schema validation; CLI test.
`cargo test -p tessera-mcp -p tessera-cli -p compositor --release`, clippy -D warnings, fmt. engine-api unchanged. Allowed: Cargo.lock, Cargo.toml, crates/tessera-mcp/**, apps/tessera-cli/**, crates/compositor/src/format.rs (only if open/save helpers are missing), tools/orchestrate/wp/M5-07/**.
