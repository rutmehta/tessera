# WP M2-21b — GPU export that never blocks the viewport (Opus)

Continue on branch wp/M2-21 (partial work committed; read tools/orchestrate/wp/M2-21/RESULTS.md first). Problems to solve:
1. **The global GPU render admission ("one renderer at a time") starves interactive rendering**: `crates/tessera-ffi/tests/develop.rs::slow_interactive_frames_are_not_starved` times out in this worktree (121 s, "no frame") while it passes on main in ~1.4 s. Replace the lock with priority-aware sharing per docs/08 §2: export renders in bands as `Priority::Export` jobs on the shared scheduler and **yields between bands** to any pending Viewport/Ui work; the device/queue are shared (wgpu queues are thread-safe), only scratch memory is bounded (keep the 512 MiB guard as a budget split: viewport reserve + export budget). Prove it with the existing starvation test plus a new test: an export batch of 5 fixtures running while the develop session drags a slider keeps slider p90 < 16 ms at L2.
2. **Default recipes fall back to CPU** because Auto lens correction / CA removal are not on the resident graph. Port the lens/geometry stage to the GPU: the composed inverse map (lens distortion + Upright + transform + crop) already exists on CPU in pipeline-cpu (from M2-09); implement it as one WGSL pass with Lanczos-3 sampling (M2-09c deferred this), lateral CA as per-channel radial scale in the same pass, vignetting as a multiply in the linear stage; gate against CPU (docs/11 §1.3 full-chain tolerance) on synthetic grids and the five fixtures with auto corrections on.
3. **Targets** (docs/08): 45 MP JPEG export < 1.5 s; 100 Web-preset JPEGs < 40 s; measure on the five fixtures with the ignored bench before/after and report.
Keep the CPU fallback (`TESSERA_EXPORT_BACKEND=cpu`) and the precision gate. `cargo test --workspace --release` (whole workspace, incl. the starvation test), clippy -D warnings on export/image-core/pipeline-gpu/pipeline-cpu/tessera-ffi, fmt. engine-api unchanged. Merge main into the branch first (M2-22 HDR and M2-18b compat landed in develop.rs/image-core/pipeline-gpu since it was cut). Commit as "wp(M2-21b): GPU export without viewport starvation, GPU lens/geometry".
