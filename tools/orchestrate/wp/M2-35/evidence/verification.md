M2-35 verification (2026-09-26)

The required command chain passed: `cargo test -p tessera-ffi --release && cargo clippy -p tessera-ffi --all-targets -- -D warnings && cargo fmt --check && (cd apps/mac && ./build-ffi.sh && swift build && swift test -c release -Xswiftc -enable-testing)`. Logs from this run were written to `/tmp/m2-35-{cargo-test,clippy,fmt,build-ffi,swift-build,swift-test}.log`. Focused `MaskingTests|ThemeLintTests|IncomingStripTests`: 15 tests, 0 failures. The real-model tomato Rust test with `TESSERA_SEGMENT_MODELS` set to the scratch model cache passed: overlay coverage 0.03845375, thumbnail coverage 0.0390625.

The app was built with `apps/mac/Support/make-app.sh` and launched with isolated `--app-dir` and the copied tomato CR3. Acceptance 57: Subject completed with `Subject mask ready`; the thumbnail has a correctly proportioned blob in the enlarged 56 × 42 point well. Screenshot: `acceptance-57-tomato-subject-window.png`. The local Exposure drag reached +0.80; the complete exposure/overlay/invert sequence was not independently verified.

For acceptance 123/125, generated sample folders (6 studio JPEGs; 8 test-camera card JPEGs) were used with `--fake-tether ... --fake-tether-interval 0 --tether --tether-connect`. Three captures appeared in Incoming. X immediately displayed Reject on #3; a single click on #1 selected it, and P immediately displayed Keep on #1 (auto-advance then focused #2). Screenshot: `acceptance-123-single-click-keep-window.png`. The frame count was typed as 5 and committed with Return; the seconds menu was set to 2 s, then Start Interval captured the remaining five frames and stopped automatically at 8 total. Screenshot: `acceptance-125-eight-frames-window.png`. An additional Capture showed the empty-card inline error in `acceptance-125-card-empty-window.png`. The double-click of #2 was not independently exercised in this run. All screenshots are app-window captures, not full-desktop captures.

The first required-chain attempt timed out in the unrelated async fallback preview test after 3 seconds. Running with the existing CI timeout proved the decode completed in about 7 seconds; the release non-CI timeout was raised to 30 seconds, while the separate nonblocking request assertion remains. Subsequent exact command chain passed.
