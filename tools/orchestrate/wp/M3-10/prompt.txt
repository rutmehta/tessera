You are implementing work package M3-10. Work only inside the worktree at /Users/rutmehta/Developer/tessera/.worktrees/M3-10. Only touch allowed paths matching: Cargo.lock,Cargo.toml,crates/agent/**,apps/tessera-cli/**,tools/orchestrate/wp/M3-10/**. IMPORTANT: the repo path contains a colon, which breaks cargo on macOS unless CARGO_TARGET_DIR points outside the repo. It is already exported in your environment as /Users/rutmehta/.cache/tessera-target/M3-10; keep it set for every cargo command (never build into ./target, never commit target/). Run this test command yourself before finishing: cargo test -p agent -p tessera-cli --release && cargo clippy -p agent -p tessera-cli --all-targets -- -D warnings && cargo fmt --check. Finish by printing a line RESULT: PASS or RESULT: FAIL <reason>.

# WP M3-10 — Agentic editing: planner + critic loop (`agent`)

Read docs/10 §2 (architecture: planner → tool API → recipe → render; critic = VLM + metrics; bounded iterations; explainability; safety), §3 phases 3–4, crates/tessera-mcp (Console, tool schemas, Author::Agent history), crates/style-profile (base edit + rationale, review queue), crates/pipeline-cpu (render for metrics), crates/ml-faces/ml-quality (perception inputs), crates/engine-api tools.rs.
Implement `crates/agent`:
- **LLM client** trait `Planner` with JSON tool-calling: providers `AnthropicMessages` (Claude, `ANTHROPIC_API_KEY`, model id configurable, default `claude-fable-5-1`), `OpenAiResponses` (`OPENAI_API_KEY`), `Ollama` (local http), and `FakePlanner` for tests (scripted plans). Use `reqwest` (rustls) — check licences. No keys are committed; when no provider is configured the agent runs in "style-profile only" mode.
- **Perception packet** per image (never pixels): scene labels placeholder (from embeddings nearest-caption if available, else histogram-derived descriptors), faces + identities, quality scores, depth availability, histogram/clipping stats, EXIF, the style-profile prediction + rationale, and a low-res render as an image attachment for VLM-capable providers.
- **Loop**: plan (ToolRequest list with rationales) → execute via Console (each step = history entry, Author::Agent, group "Agent base edit") → render preview → **critic**: objective metrics (clipping %, mean luminance vs target, contrast distribution, skin-tone ΔE vs a target band using face boxes, noise) + optional VLM judgement → accept / revise, max N iterations (default 3), with a per-image time budget. Guardrails from docs/10: no generative pixels, retouch limited to configured allowances (default: no body reshaping), every step reversible, provenance record ("AI-assisted, non-generative edits") stored in the recipe extension.
- **Batch mode** over a shoot with consistency constraints (reuse style-profile's grouping) and a review queue by critic confidence; natural-language redo per image ("warmer, keep the sky") → a constrained re-plan that touches only the relevant tools.
- **CLI**: `tessera agent edit <image|dir> [--provider ...] [--dry-run]` prints the plan and rationales; `--redo "instruction"`.
- Tests (FakePlanner + fixtures): a scripted plan produces the expected history entries with rationales; critic rejects a plan that clips > 5% highlights and the loop revises; guardrail blocks a disallowed tool; batch consistency; redo touches only WB tools; provider request/response serialisation tests with recorded JSON (no network).
`cargo test -p agent -p tessera-cli --release`, clippy -D warnings, fmt. engine-api unchanged (report needed fields). Allowed: Cargo.lock, Cargo.toml, crates/agent/**, apps/tessera-cli/**, tools/orchestrate/wp/M3-10/**.
