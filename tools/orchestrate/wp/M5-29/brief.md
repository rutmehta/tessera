# WP M5-29 — Wire retouch and neural filters into the document tool surface

Read crates/filters/src/compositor_adapter.rs (CompositorFilters: names → effects; liquify/CAF/remove/camera_raw already registered), crates/ml-filters (catalog(), NeuralFilter trait: skin_smoothing, colorize, jpeg_artifact_removal), crates/filters/src/remove.rs + distraction.rs (LaMa Remove, DistractionDetector), crates/tessera-mcp/src/documents/** (document tools: how smart filters / filters are exposed as MCP tools and Actions; wiring tests), crates/engine-api (DocumentToolCall variants for filters), crates/tessera-ffi/src/document.rs (DocumentSession filter APIs that Machine B's UI calls; see apps/mac/Sources/TesseraCore/Document*.swift for the consumer shape — do not edit Swift).
1. Register the ml-filters catalog in `CompositorFilters` as smart filters (names `neural/skin_smoothing`, `neural/colorize`, `neural/jpeg_artifact_removal`) with strict params, weights-missing → clear Unsupported error (never a silent no-op), and GPU routing declared unsupported (CPU fallback per layer via the M5-23 mechanism).
2. Document tools: `remove_object` (mask → LaMa or PatchMatch per RemoveParams), `remove_distractions` (detector → masks → remove; returns what was removed), `content_aware_fill`, `content_aware_move`, `liquify` (mesh payload), `camera_raw_filter`, and the three neural filters — each as an MCP tool + Action descriptor + DocumentToolCall dispatch with the existing expect_head/rationale/history-group envelope; add engine-api variants if needed (backward compatible; bump the minor version and changelog).
3. tessera-ffi DocumentSession: expose the same operations (apply as smart filter or destructively to a raster layer) with UniFFI records; regenerate bindings; keep `swift build` green (no UI work).
4. Wiring tests in tessera-mcp and tessera-ffi (CPU PatchMatch path so no weights are needed; neural filters tested for the clean weights-missing error).
Gate: `cargo test -p filters -p tessera-mcp -p tessera-ffi -p engine-api --release && cargo clippy -p filters -p tessera-mcp -p tessera-ffi -p engine-api --all-targets -- -D warnings && cargo fmt --check && cargo check --workspace && (cd apps/mac && ./build-ffi.sh && swift build)`. Allowed: Cargo.lock,crates/filters/Cargo.toml,crates/filters/src/compositor_adapter.rs,crates/filters/src/lib.rs,crates/filters/tests/**,crates/tessera-mcp/**,crates/engine-api/**,crates/tessera-ffi/src/document.rs,crates/tessera-ffi/src/document/**,crates/tessera-ffi/src/lib.rs,crates/tessera-ffi/tests/**,crates/tessera-ffi/Cargo.toml,apps/mac/Sources/CTesseraFFI/**,apps/mac/Sources/TesseraFFI/**,tools/orchestrate/wp/M5-29/**. Another package is editing crates/export and crates/tessera-ffi/src/export.rs concurrently; do not touch those.
