# WP M2-42 — People FFI follow-ups (from the People view report)

Read tools/orchestrate/wp/M2-40/REPORT.md ("FFI gaps"), crates/tessera-ffi/src/assist.rs (people section ~lines 900–1100 and the PersonInfo/PeopleJobResult records), crates/ml-faces/src/people.rs + clustering.rs, crates/index/src/people.rs, crates/engine-api/src/people.rs (1.3 PersonSummary etc.), and apps/mac/Sources/TesseraCore/Assist/People.swift (the Swift consumer; do not edit it, but keep it compiling — the UniFFI bindings are regenerated by apps/mac/build-ffi.sh).
1. `PersonInfo` gains `named: bool`, `confirmed_count: u32`, `face_count: u32`, and `medoid_face: Option<PersonFace>` (the true medoid from the index, not the sharpest face) so the app no longer calls `person_assignments` per image.
2. `PeopleJobResult` gains `sample_size: u32` (the actual reservoir size when `approximate`).
3. `person_members(person_id) -> Vec<PersonFace>` in one call.
4. Undo: people edits (assign, confirm, merge, split, name) record an inverse in a small per-session people undo stack exposed as `undo_people_edit() -> Option<String>` / `redo_people_edit()` (description strings for the menu); merge undo restores the source person id and its members.
Backward compatible for existing Swift call sites (add fields with defaults where UniFFI allows; otherwise update the Swift consumer minimally and say so). Tests in crates/tessera-ffi/tests/assist.rs for each addition (seeded faces fixture path as used by existing tests). Gate: `cargo test -p tessera-ffi --release --lib --test assist && cargo clippy -p tessera-ffi --all-targets -- -D warnings && cargo fmt --check && (cd apps/mac && ./build-ffi.sh && swift build && swift test -c release -Xswiftc -enable-testing --filter People)`. Allowed: crates/tessera-ffi/src/assist.rs,crates/tessera-ffi/src/lib.rs,crates/tessera-ffi/src/tessera.udl,crates/tessera-ffi/tests/assist.rs,crates/index/src/people.rs,crates/index/tests/people.rs,crates/ml-faces/src/people.rs,apps/mac/Sources/CTesseraFFI/**,apps/mac/Sources/TesseraFFI/**,apps/mac/Sources/TesseraCore/Assist/People.swift,apps/mac/Tests/TesseraCoreTests/PeopleTests.swift,tools/orchestrate/wp/M2-42/**.
