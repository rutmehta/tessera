You are implementing work package M2-07c. Work only inside the worktree at /Users/rutmehta/Developer/tessera/.worktrees/M2-07c. Only touch allowed paths matching: apps/mac/**,tools/orchestrate/wp/M2-07c/**. IMPORTANT: the repo path contains a colon, which breaks cargo on macOS unless CARGO_TARGET_DIR points outside the repo. It is already exported in your environment as /Users/rutmehta/.cache/tessera-target/M2-07c; keep it set for every cargo command (never build into ./target, never commit target/). Run this test command yourself before finishing: cd apps/mac && ./build-ffi.sh && swift build && swift test && bash Support/make-app.sh release && bash Support/release/test-release.sh. Finish by printing a line RESULT: PASS or RESULT: FAIL <reason>.

# WP M2-07c — Sparkle must never block or crash the app

Findings from two app packages: (1) a bundle from `make-app.sh` fails to load Sparkle when launched directly on this Mac (framework load / code-signature issue with ad-hoc signing and the embedded XPC services); (2) with `SUPublicEDKey` empty, Sparkle's updater refuses to start and shows "Unable to Check For Updates" as a modal that blocks input; in the bare debug binary the same dialog appears.
Fix in apps/mac: initialise `SPUStandardUpdaterController` lazily and only when `SUPublicEDKey` is non-empty and `SUFeedURL` is set; otherwise hide/disable "Check for Updates…" with a tooltip "updates not configured for this build" and log once. Never present a modal at launch. Fix the bundle so a directly launched `build/Tessera.app` (ad-hoc signed) loads Sparkle: verify with `codesign -dvv` on the framework and XPCs, fix `make-app.sh` (rpath, framework signing order: XPCs → framework → app; `--options runtime` with the entitlements), and add a test in `Support/release/test-release.sh` that launches the built bundle with `--develop-selftest` for 5 s and asserts it exited cleanly with the selftest line printed. Document in Support/release/README.md.
`(cd apps/mac && ./build-ffi.sh && swift build && swift test && bash Support/make-app.sh release && bash Support/release/test-release.sh)`. Only touch apps/mac/** and tools/orchestrate/wp/M2-07c/**.
