# WP M2-40 — People view: named identities, naming, merge/split, per-person filter (app)

Read docs/01 §1.4 (People view), apps/mac/DESIGN.md + Theme.swift + Components.swift (design system; theme lint must stay green), apps/mac/Sources/Tessera/Cull/FaceStripView.swift and AssistController.swift (existing face strip), apps/mac/Sources/Tessera/Library (FilterBar, LibraryModel), Sidebar/, crates/tessera-ffi/src/assist.rs lines 900–1100 (people(), refresh_people, person_assignments, assign/confirm/merge/split/name_person, people_name_suggestions, frames_with_person) and tools/orchestrate/wp/M3-19/implementation.md (boundaries: approximate clustering, opt-in sidecar/keyword writes).
Build:
1. **People** entry in the sidebar → a grid of person tiles (medoid face crop, name or "Unnamed", count, confirmed badge), sorted named-first then by count; unnamed cluster tiles show a name field with suggestions (from `people_name_suggestions`) as a menu; Return commits `name_person` (default options: no sidecar write; a Settings ▸ Library toggle "Write face regions to XMP" and "Add person keywords" flips the opt-ins).
2. Multi-select tiles → toolbar **Merge**; a person's detail view (double-click) lists its faces as chips with confirm/unconfirm toggles and a **Split** (selected faces → new person); drag a face chip onto another person tile to reassign. All actions go through the FFI calls above and refresh from `people(refresh: true)`.
3. Filter bar facet **Person** (multi-select names; intersects with existing facets) using `frames_with_person`; loupe face strip hover/tooltip shows the person's name when known and offers "Name…" in its context menu.
4. Run `refresh_people(force: false)` when the People view opens and after Analyze Faces; show the `approximate` flag as a subtle footnote ("Clustered from a sample of N faces").
Accessibility identifiers on every control (see ACCEPTANCE.md Identifiers appendix); add ACCEPTANCE.md steps (People view, name, merge, split, filter) with a fake/generated fixture note; Swift tests for the view model (naming, merge, split, filter intersection) with a stubbed engine.
Gate: `(cd apps/mac && ./build-ffi.sh && swift build && swift test -c release -Xswiftc -enable-testing)`. Allowed: apps/mac/** except Sources/CTesseraFFI and Sources/TesseraFFI (regenerated), crates/tessera-ffi/src/assist.rs only if an FFI gap blocks you (say so in the report), tools/orchestrate/wp/M2-40/**. Commit on branch wp/M2-40 in the worktree; do not merge; end your report with RESULT: PASS or FAIL and what was not verified.
