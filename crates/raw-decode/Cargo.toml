[package]
name = "raw-decode"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"

[lib]
path = "src/lib.rs"

[dependencies]
engine-api = { path = "../engine-api" }
libraw-ffi = { path = "../libraw-ffi" }

[dev-dependencies]
criterion = "0.5"

[[bench]]
name = "decode"
harness = false
