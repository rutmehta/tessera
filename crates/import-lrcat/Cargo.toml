[package]
name = "import-lrcat"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"

[lib]
path = "src/lib.rs"

[dependencies]
library = { path = "../library" }
engine-api = { path = "../engine-api" }
sidecar = { path = "../sidecar" }
rusqlite.workspace = true
serde.workspace = true
serde_json.workspace = true
tempfile.workspace = true
roxmltree = "0.20"
image = { workspace = true, optional = true }

[features]
# Synthetic catalog + JPEG originals + Previews.lrdata (tests, CLI --make-fixture).
fixture = ["dep:image"]

[dev-dependencies]
import-lrcat = { path = ".", features = ["fixture"] }
