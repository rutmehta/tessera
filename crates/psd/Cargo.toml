[package]
name = "psd"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Bounded PSD/PSB reader and preserving writer"

[dependencies]
flate2 = { version = "1", default-features = false, features = ["rust_backend"] }
